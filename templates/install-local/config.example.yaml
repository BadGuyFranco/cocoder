$schema: ../../packages/schemas/dist/install-config.schema.json
version: "0.1"
defaults:
  adapter: codex
oz:
  host: 127.0.0.1
  port: 7878
theme:
  mode: system
secrets:
  openai: ${env:OPENAI_API_KEY}
