# local/secrets/ holds machine-local secrets (API keys, Oz session token, etc.).
# The directory is shipped via this template so `cocoder init` lands the
# correct ignore rule. Adopters should NEVER commit secret material.
#
# Anything in this directory is gitignored except this file itself.
*
!.gitignore
