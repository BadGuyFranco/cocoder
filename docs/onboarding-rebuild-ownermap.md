# Onboarding Rebuild Owner Map

Scope: analysis only for ADR-0026's rebuild of existing-repo onboarding as an Oscar-driven priority. This file maps the current owner surfaces so the rebuild can delete the standalone executor without deleting reusable audit tooling.

## Evidence Commands

- `git ls-files | grep -i takeover` returned `packages/personas/base/playbooks/cocoder-takeover.md` and `scripts/proof-takeover-executor.mjs`.
- `grep -rliE 'takeover' packages cocoder` found the package/governance files listed in the rename inventory below.
- `grep -rn 'cocoder-takeover' packages cocoder` found the id pins listed in the rename inventory below.
- `grep -rniE 'takeover' packages cocoder | awk ...` produced the per-file line summary used in the rename inventory.

## 1. RETIRE - Standalone Executor Runner-Mode

| Unit | Exported symbols | Current callers | Why retired |
|---|---|---|---|
| `packages/core/src/playbooks/executor.ts` | `PlaybookExecutorStatus`, `PlaybookGateState`, `PlaybookExecutorState`, `PlaybookPhaseActionInput`, `PlaybookPhaseAction`, `PlaybookExecutorDeps`, `StartPlaybookExecutorInput`, `ResumePlaybookExecutorInput`, `LoadPlaybookExecutorInput`, `FounderApproval`, `PlaybookExecutorResult`, `LoadedPlaybookExecutor`, `readPlaybookExecutorState`, `startPlaybookExecutor`, `resumePlaybookExecutor`, `loadPlaybookExecutor` (`packages/core/src/playbooks/executor.ts:5-74`, `packages/core/src/playbooks/executor.ts:174-199`). | Re-exported by `packages/core/src/playbooks/index.ts:9-23` and `packages/core/src/index.ts:105-145`; launched by daemon playbook branch via `startPlaybookExecutor` (`packages/daemon/src/launcher.ts:616-628`); resumed only by tests/proof via `loadPlaybookExecutor` (`packages/daemon/tests/mutations.test.ts:832-921`, `packages/core/tests/playbook-executor.test.ts:77-100`, `scripts/proof-takeover-executor.mjs:194-212`). | ADR-0026 supersedes the phase-executor runner-mode, specifically the phase cursor and `awaiting-founder` typed-resume mechanism (`cocoder/decisions/0026-onboard-existing-as-oscar-priority.md:1-8`, `cocoder/decisions/0026-onboard-existing-as-oscar-priority.md:72-78`). |
| Phase cursor / state file | Internal `STATE_FILE`, `statePath`, `stateFor`, `currentPhaseId`, `persist`, `assertPlaybookMatchesState`, `drive`, `initialState`, `parseState` are not exported (`packages/core/src/playbooks/executor.ts:74-171`). | Indirectly exercised only through executor exports above. The daemon records the executor state path event after start (`packages/daemon/src/launcher.ts:621-627`); tests read `playbook-state.json` directly (`packages/core/tests/playbook-executor.test.ts:15-16`, `packages/daemon/tests/mutations.test.ts:548-552`). | This is the deterministic P0-P7 cursor ADR-0026 replaces with ordinary Oscar priority state/wraps. |
| `awaiting-founder` gate + typed resume payload | Exported as `PlaybookExecutorStatus`, `PlaybookGateState`, `FounderApproval`, and the `resume` API on `LoadedPlaybookExecutor` (`packages/core/src/playbooks/executor.ts:5-16`, `packages/core/src/playbooks/executor.ts:58-72`, `packages/core/src/playbooks/executor.ts:184-199`). | Daemon maps a paused executor to run status `awaiting-founder` after start (`packages/daemon/src/launcher.ts:621-628`). Tests/proof call `resume({ approvedBy, note })` to advance gates (`packages/core/tests/playbook-executor.test.ts:89-100`, `packages/daemon/tests/mutations.test.ts:849-921`, `scripts/proof-takeover-executor.mjs:197-212`). | ADR-0026 says this duplicated founder interaction and had no real surface (`cocoder/decisions/0026-onboard-existing-as-oscar-priority.md:19-45`, `cocoder/decisions/0026-onboard-existing-as-oscar-priority.md:72-78`). |
| Bespoke executor gate semantics | `drive` pauses whenever parsed playbook phase metadata has `founderGate` (`packages/core/src/playbooks/executor.ts:117-146`); `approvedState` advances the cursor only from `awaiting-founder` with `FounderApproval` (`packages/core/src/playbooks/executor.ts:153-164`). | The loader parses `founderGate` from the baked phase table marker (`packages/core/src/playbooks/loader.ts:159-192`); `playbooks.test.ts` pins the takeover P1/P4/P6 gate table (`packages/core/tests/playbooks.test.ts:55-64`). | ADR-0026 moves P1/P4/P6 founder interaction into the Oscar-founder loop, so these executor-only gates retire, while the audit concepts may survive as priority beats (`cocoder/decisions/0026-onboard-existing-as-oscar-priority.md:47-61`). |
| Daemon playbook launch branch | `launchRun`'s playbook branch creates an `onboarding-playbook` run, composes `createDaemonPlaybookPhaseAction`, calls `startPlaybookExecutor`, records `playbook-executor`, and sets `awaiting-founder` (`packages/daemon/src/launcher.ts:193-204`, `packages/daemon/src/launcher.ts:597-628`). | Called from `POST /runs` after route validation of exactly one target (`packages/daemon/src/routes.ts:93-116`, `packages/daemon/src/launcher.ts:470-530`). | This branch is the daemon entry point for the retired runner-mode; Oscar-driven onboarding should launch as an ordinary priority, not `target.kind === 'playbook'` executor. |
| `scripts/proof-takeover-executor.mjs` | Script, no package export. Imports `startPlaybookExecutor`, `loadPlaybookExecutor`, `loadOnboardingPlaybooks`, `scaffoldCocoderZone`, and `AuditWriteBoundaryError` (`scripts/proof-takeover-executor.mjs:19-25`). | Founder/manual proof only; it creates `cocoder-takeover` runs and resumes P1/P4/P6 directly (`scripts/proof-takeover-executor.mjs:164-212`, `scripts/proof-takeover-executor.mjs:230-273`). | ADR-0026 names this proof as a retired artifact (`cocoder/decisions/0026-onboard-existing-as-oscar-priority.md:72-78`). |

## 2. KEEP-AS-TOOLING - Reusable Audit Machinery

These units should survive, but most phase adapters currently have no production caller except the retired daemon executor composition. The rebuild must re-wire them as Oscar atom tools or ordinary priority helpers.

| Unit | Exported symbols | Current invokers | Orphan risk after executor deletion |
|---|---|---|---|
| Deep-read Play | `packages/personas/base/plays/deep-read.md` has id `deep-read` (found by `rg deep-read`; loaded by tests and launcher). | Loaded by `createDaemonPlaybookPhaseAction` for P2/P3 (`packages/daemon/src/launcher.ts:313-343`); surfaced in play read tests (`packages/daemon/tests/read-surfaces.test.ts:251`); used by unit tests around the play and P2/P3 dispatch. | Not orphaned as a Play because play surfaces/tests still load it; production onboarding use is orphaned unless Oscar atoms call it. |
| P2 fanout engine | `combineSourcePair`, `parseDeepReadIterationResult`, `runDeepReadSource` and types (`packages/core/src/playbooks/index.ts:165-191`; implementation in `packages/core/src/playbooks/p2-fanout.ts:153-193`, `packages/core/src/playbooks/p2-fanout.ts:227-235`). | Called by `runPlaybookP2Action` (`packages/core/src/playbooks/p2-action.ts:8-10`, `packages/core/src/playbooks/p2-action.ts:67-104`) and P3 follow-up parsing (`packages/core/src/playbooks/p3-action.ts:202-214`). | Engine has unit tests; production caller is the retired P2 phase adapter unless the rebuild wires Oscar atoms to it. |
| P2 dispatch helper | `createDeepReadTurn`, `resolveDeepReadAssignments` and assignment/top-tier types (`packages/core/src/playbooks/index.ts:192-201`; `packages/core/src/playbooks/p2-dispatch.ts:12-87`). | Called by P2 action (`packages/core/src/playbooks/p2-action.ts:67-88`) and P3 action (`packages/core/src/playbooks/p3-action.ts:176-214`). | Same as above: useful helper, but production path currently enters through the retired daemon executor. |
| P3 cross-check | `buildRound` (`packages/core/src/playbooks/p3-cross-check.ts:1-30`). | Called by `runPlaybookP3Action` (`packages/core/src/playbooks/p3-action.ts:10-12`, `packages/core/src/playbooks/p3-action.ts:117-173`). | Pure engine has tests; production caller is orphaned with executor unless Oscar invokes P3 action/tooling. |
| Recon inventory | `inventoryRepo` and inventory types (`packages/core/src/playbooks/index.ts:24-37`; `packages/core/src/playbooks/recon.ts:1-38`). | Called inside P1 action (`packages/core/src/playbooks/p1-action.ts:52-55`). | Orphaned from production if P1 phase adapter is deleted and no ordinary priority helper calls it. |
| Recon pass | `buildAgenticReconPrompt`, `runAgenticRecon`, `parseSubsystemsJsonPayload`, recon/subsystem types (`packages/core/src/playbooks/index.ts:38-51`; `packages/core/src/playbooks/recon-pass.ts:58-93`). | `runAgenticRecon` is called by P1 action (`packages/core/src/playbooks/p1-action.ts:56-59`); `parseSubsystemsJsonPayload` is called by P2/P3 readers (`packages/core/src/playbooks/p2-action.ts:49-50`, `packages/core/src/playbooks/p3-input.ts:23-28`). | Keep; production caller must move out of executor-driven P1/P2/P3. |
| Intent | `buildIntentIntakePrompt`, `runIntentIntake`, intent types (`packages/core/src/playbooks/index.ts:150-164`; `packages/core/src/playbooks/intent.ts:66-89`). | Called by P1 action (`packages/core/src/playbooks/p1-action.ts:60-64`); unit tests call it directly (`packages/core/tests/playbook-intent.test.ts:20-56`). | The engine survives; prompt text currently says `P1 Takeover` and must rename on surviving surface (`packages/core/src/playbooks/intent.ts:78-89`). |
| Intent artifacts | `defaultIntentGitReader`, `enumerateIntentArtifacts` and artifact/git types (`packages/core/src/playbooks/index.ts:202-210`; `packages/core/src/playbooks/intent-artifacts.ts:50-82`). | Called by P1 action (`packages/core/src/playbooks/p1-action.ts:52-55`); direct unit tests (`packages/core/tests/playbook-intent-artifacts.test.ts:28-49`). | Keep; production caller must move from retired P1 action wrapper or the action must get a new non-executor caller. |
| Estimate | `P2_CAPS`, `P3_CAPS`, `WORKING_DAY_MINUTES`, `buildEstimate`, `summarizeEstimate`, estimate/allocation types (`packages/core/src/playbooks/index.ts:52-71`; `packages/core/src/playbooks/estimate.ts:108-170`). | Called by P1 action (`packages/core/src/playbooks/p1-action.ts:65-66`) and P2/P3 input parsing (`packages/core/src/playbooks/p2-action.ts:7`, `packages/core/src/playbooks/p3-input.ts:3-10`). | Keep; production use must be re-wired to Oscar priority atoms. |
| P4 questions | `buildFounderQuestions`, question payload/item types (`packages/core/src/playbooks/index.ts:132-137`; `packages/core/src/playbooks/p4-questions.ts:5-33`). | Called by P4 action (`packages/core/src/playbooks/p4-action.ts:35-50`); direct unit tests. | Keep as a question generator, but not as an executor hard gate. |
| P5 synthesis | `synthesizeP5Governance` and P5 payload/objective/priority types (`packages/core/src/playbooks/index.ts:139-147`; `packages/core/src/playbooks/p5-synthesis.ts:5-66`). | Called by P5 action (`packages/core/src/playbooks/p5-action.ts:36-67`); direct unit tests. | Keep; currently reads a founder checkpoint from executor gate metadata via P5 action (`packages/core/src/playbooks/p5-action.ts:29-36`, `packages/core/src/playbooks/p5-action.ts:69-72`), so new caller must pass founder context differently. |
| P6 apply | `applyP6Governance`, `approvalFromP6Gate`, `createPlaybookP6PhaseAction`, `runPlaybookP6Action`, P6 types (`packages/core/src/playbooks/index.ts:118-131`; `packages/core/src/playbooks/p6-apply.ts:9-82`, `packages/core/src/playbooks/p6-apply.ts:124-168`). | P6 present is called by `createDaemonPlaybookPhaseAction` (`packages/daemon/src/launcher.ts:355-365`); P7 apply path calls `approvalFromP6Gate` and `applyP6Governance` (`packages/daemon/src/launcher.ts:366-384`); direct unit tests (`packages/core/tests/playbook-p6-apply.test.ts:75-113`). | `applyP6Governance` survives; `approvalFromP6Gate` is executor-gate-specific and should retire or be replaced by ordinary verify/wrap ratification input. |
| Input/render helpers | P3/P4/P5/P6 readers and renderers: `readP3InputArtifacts`, `readP4InputArtifacts`, `readP5InputArtifacts`, `readP6Synthesis`, `renderCrossCheckMarkdown`, `renderFounderQuestionsMarkdown`, `renderP5*`, `renderP6*` (`packages/core/src/playbooks/p3-input.ts:7-28`, `packages/core/src/playbooks/p4-input.ts:7-21`, `packages/core/src/playbooks/p5-input.ts:8-23`, `packages/core/src/playbooks/p6-input.ts:5-26`, `packages/core/src/playbooks/p3-render.ts:1-23`, `packages/core/src/playbooks/p4-render.ts:1-27`, `packages/core/src/playbooks/p5-render.ts:1-64`, `packages/core/src/playbooks/p6-render.ts:1-38`). | Called by P3/P4/P5/P6 actions (`packages/core/src/playbooks/p3-action.ts:117-173`, `packages/core/src/playbooks/p4-action.ts:35-50`, `packages/core/src/playbooks/p5-action.ts:36-67`, `packages/core/src/playbooks/p6-apply.ts:69-82`). | Keep, but `p5-input.ts` documents that P4 answers currently exist only in `playbook-state.json` (`packages/core/src/playbooks/p5-input.ts:16-23`), which is an executor-state coupling the rebuild must replace. |
| Phase action wrappers | `createPlaybookPhaseAction`, `createPlaybookP2PhaseAction`, `createPlaybookP3PhaseAction`, `createPlaybookP4PhaseAction`, `createPlaybookP5PhaseAction`, `createPlaybookP6PhaseAction`, and `runPlaybookP*Action` exports (`packages/core/src/playbooks/index.ts:72-131`). | Composed in daemon `createDaemonPlaybookPhaseAction` (`packages/daemon/src/launcher.ts:306-385`). | The `runPlaybookP*Action` functions can survive as tools. The `createPlaybookP*PhaseAction` wrappers import `PlaybookPhaseAction` and `OnboardingPlaybookPhase` (`packages/core/src/playbooks/p1-action.ts:6-10`, `packages/core/src/playbooks/p2-action.ts:5-10`, `packages/core/src/playbooks/p3-action.ts:6-12`, `packages/core/src/playbooks/p4-action.ts:3-7`, `packages/core/src/playbooks/p5-action.ts:3-7`, `packages/core/src/playbooks/p6-apply.ts:3-7`) and become dead/orphaned if the executor phase protocol is deleted. |

## 3. KEEP-AS-IS - Orthogonal Surfaces

| Unit | Exported symbols | Current invokers | Executor dependency check |
|---|---|---|---|
| Scaffold primitive | `scaffoldCocoderZone`, `installRoot`, `workspaceTemplateDir`, scaffold types (`packages/core/src/scaffold/scaffold.ts:44-69`; re-exported by `packages/core/src/scaffold/index.ts:1-7` and `packages/core/src/index.ts:183-189`). | Workspace creation route scaffolds governance directly (`packages/daemon/src/routes.ts:340-346`); scaffold tests call it directly; proof script also exercises it historically (`scripts/proof-takeover-executor.mjs:265-267`). | No import of `executor.ts`; it is a create-only file copy primitive (`packages/core/src/scaffold/scaffold.ts:1-2`). Keep unchanged. |
| Audit write boundary | `AuditWriteBoundaryError`, `runCommitGate`, `AuditWriteBoundary` (`packages/core/src/commit-gate/gate.ts:15-29`, `packages/core/src/commit-gate/gate.ts:41-57`, `packages/core/src/commit-gate/gate.ts:57-90`; re-exported by `packages/core/src/index.ts:207-218`). | Used by daemon P7 apply and post-wrap support commit when playbook id is old takeover (`packages/daemon/src/launcher.ts:366-384`, `packages/daemon/src/launcher.ts:878-889`); commit-gate tests prove refusal (`packages/core/tests/commit-gate.test.ts:178-229`); proof script checks the error (`scripts/proof-takeover-executor.mjs:230-250`). | Core gate has no executor import; only callers use old id wiring. Keep the core boundary and rename/re-wire labels/callers. |
| Loader discovery surface | `loadOnboardingPlaybooks` and playbook types (`packages/core/src/playbooks/loader.ts:7-73`, `packages/core/src/playbooks/loader.ts:195-225`; re-exported by `packages/core/src/playbooks/index.ts:1-8` and `packages/core/src/index.ts:105-106`). | Daemon priority surface maps loaded playbooks into `onboarding` summaries (`packages/daemon/src/priority-order.ts:15-31`); route returns `onboarding` from `GET /workspaces/:id/priorities` (`packages/daemon/src/routes.ts:354-359`); launcher validates requested playbook ids (`packages/daemon/src/launcher.ts:520-529`); loader/read tests pin it (`packages/core/tests/playbooks.test.ts:8-64`, `packages/daemon/tests/read-surfaces.test.ts:174-187`). | Loader has no executor import and only parses markdown metadata/table (`packages/core/src/playbooks/loader.ts:1-5`, `packages/core/src/playbooks/loader.ts:159-225`). Keep the discovery surface, but the baked phase table is a break edge because executor semantics currently consume it. |

## 4. RENAME Inventory

Classification meanings:

- RETIRE: occurrence lives in code/test/proof for the standalone executor driver being deleted. Do not rename unless needed temporarily; it should disappear.
- RENAME: occurrence lives on a surviving package or active governance surface and must change to `onboard-existing` / `Onboard (existing repo)` / a non-takeover mode name.
- HISTORICAL: provenance record (past ADR/session narrative) should stay as written.

| Classification | Occurrence(s) | Evidence |
|---|---|---|
| RETIRE | Executor proof script path and content. | `git ls-files | grep -i takeover` found `scripts/proof-takeover-executor.mjs`; the proof imports executor/scaffold/boundary helpers and drives direct resumes (`scripts/proof-takeover-executor.mjs:19-25`, `scripts/proof-takeover-executor.mjs:164-212`, `scripts/proof-takeover-executor.mjs:230-273`). |
| RETIRE | Executor unit tests using `mode: 'takeover'`, takeover fixtures, `awaiting-founder`, and direct resume. | `packages/core/tests/playbook-executor.test.ts:26,59,106,112,117-119,157,188`; core executor test imports and exercises `startPlaybookExecutor`/`loadPlaybookExecutor` (`packages/core/tests/playbook-executor.test.ts:5-16`, `packages/core/tests/playbook-executor.test.ts:54-100`, `packages/core/tests/playbook-executor.test.ts:175-197`). |
| RETIRE | Daemon executor E2E tests for `POST /runs` with `playbookId: 'cocoder-takeover'`, `awaiting-founder`, manual `loadPlaybookExecutor` resumes, P7 apply. | `packages/daemon/tests/mutations.test.ts:665-731`, `packages/daemon/tests/mutations.test.ts:736-935`; grep lines `665,666,692,714,724,727,731,736,737,762,768,773,803,811,820,832`. |
| RETIRE | Daemon launcher's old playbook-specific P7 apply and post-wrap support boundary keyed by `cocoder-takeover`. | `packages/daemon/src/launcher.ts:366-384`, `packages/daemon/src/launcher.ts:878-889`; grep lines `366,379,381,888`. The hard boundary behavior survives, but these old id/gate hooks are part of the executor path. |
| RENAME | Shipped playbook skeleton file and frontmatter: `packages/personas/base/playbooks/cocoder-takeover.md` must rename to `onboard-existing.md` and update id/title/mode. | `packages/personas/base/playbooks/cocoder-takeover.md:1-7`, `packages/personas/base/playbooks/cocoder-takeover.md:13-24`, `packages/personas/base/playbooks/cocoder-takeover.md:26-43`; grep lines `2,3,5`. |
| RENAME | Playbook README index row. | `packages/personas/base/playbooks/README.md:12-16`; grep line `15`. |
| RENAME | Loader mode union and parser allow `takeover`; surviving loader surface should rename the mode. | `packages/core/src/playbooks/loader.ts:7`, `packages/core/src/playbooks/loader.ts:108-110`; grep lines `7,109`. |
| RENAME | Daemon onboarding summary type exposes `mode: 'takeover'`. | `packages/daemon/src/priority-order.ts:15-20`; grep line `18`. |
| RENAME | Loader/read-surface tests pin old id/title/mode and onboarding payload. | `packages/core/tests/playbooks.test.ts:13-29`, `packages/core/tests/playbooks.test.ts:55-64`; `packages/daemon/tests/read-surfaces.test.ts:180-185`. |
| RENAME | Commit-gate tests pin old id/label/message. Keep boundary semantics, rename label/id/message. | `packages/core/tests/commit-gate.test.ts:178-229`; grep lines `178,181,196,201,207,220,222,228`. |
| RENAME | Intent prompt and tests still say `P1 Takeover Intent Intake` / "takeover path". | `packages/core/src/playbooks/intent.ts:78-89`; `packages/core/tests/playbook-intent.test.ts:28,36,52`; `packages/core/tests/playbook-intent-artifacts.test.ts:17,34`. |
| RENAME | Pure P6 test suite title says "P6 takeover ratification". | `packages/core/tests/playbook-p6-apply.test.ts:75`. |
| RENAME | New Primary docs warn not to import "Takeover" machinery; surviving wording should use Onboard Existing or deep-audit. | `packages/personas/base/playbooks/new-primary.md:32-35`; `packages/personas/base/playbooks/new-primary-tech-stack.md:102-103`. |
| RENAME | Active roadmap/priority governance should be updated as part of the rebuild and rename. | `cocoder/PLAYBOOK.md:226,229,235,237`; `cocoder/priorities/new-primary-root.md:3,10,11,27,38,42,43,81,85,86,94,95,106,113,117,118,125,126,129,132,133,135,143,146,147,152,153,160,161,162,168,176,182,183,188,213,231,232,249,316,363,382,403,431,480,526,553,620,631,635,671,685,697,705,720,721,722,724,727,762,766,787,788,826,831,837,838,886,887,888,892,893`; open ticket wording if still active: `cocoder/tickets/open/0014-oz-workspace-path-picker.md:18,25`. |
| HISTORICAL | Session log records prior runs/commits/proofs and should preserve old wording. | `cocoder/SESSION_LOG.md:101,105,106,108,109,115,119,146,176,189,311,331,332,585,625,627`. |
| HISTORICAL | Past ADR/product history and the superseded phase-executor addendum should preserve old wording, except for explicit current notices already added. | `cocoder/decisions/0010-taxonomy-and-authoring.md:5`; `cocoder/decisions/0020-primary-root-audit.md:1,4,6,62,65,72,74,116,135,140,144,154`; `cocoder/decisions/0020-addendum-phase-executor.md:8,16,29,67,120,144,164,165,227,250,259,268,276,459,485,517,527,561,593,613,630,646,655,661,667,668`; `cocoder/decisions/0026-onboard-existing-as-oscar-priority.md:1,19,63,66,78`; `cocoder/decisions/README.md:30,33`. |

No `UNCLASSIFIED` occurrences remain from the grep summary.

## 5. Dependency Edges / Break-Risk

| Edge | Break risk | Evidence |
|---|---|---|
| Delete `executor.ts` without removing/replacing public exports. | Typecheck breaks through re-exports in `packages/core/src/playbooks/index.ts` and `packages/core/src/index.ts`; daemon/tests import executor symbols. | Re-exports: `packages/core/src/playbooks/index.ts:9-23`, `packages/core/src/index.ts:105-145`; imports/callers: `packages/daemon/src/launcher.ts:42`, `packages/daemon/src/launcher.ts:621`, `packages/daemon/tests/mutations.test.ts:10`, `packages/core/tests/playbook-executor.test.ts:5-13`. |
| Delete executor without rewriting daemon `target.kind === 'playbook'` launch branch. | `POST /runs` for onboarding playbooks still validates ids and then calls `startPlaybookExecutor`; route can create runs that no longer have a driver. | Target type and playbook validation: `packages/daemon/src/launcher.ts:201-204`, `packages/daemon/src/launcher.ts:520-529`; executor launch: `packages/daemon/src/launcher.ts:597-628`; route launch body supports `playbookId`: `packages/daemon/src/routes.ts:78-116`. |
| Rename skeleton file/id without updating loader tests and daemon read-surface expectations. | Loader derives id from filename and requires frontmatter id to match; tests pin `cocoder-takeover` and mode `takeover`. | Loader file-id coupling: `packages/core/src/playbooks/loader.ts:195-205`, `packages/core/src/playbooks/loader.ts:211-221`; tests: `packages/core/tests/playbooks.test.ts:13-29`, `packages/daemon/tests/read-surfaces.test.ts:183-185`. |
| Keep loader phase table but remove executor gate semantics. | The baked table still parses `founderGate`; old tests assert P1/P4/P6 gates even though ADR-0026 moves those to Oscar's normal loop. | Loader phase parse: `packages/core/src/playbooks/loader.ts:159-192`; gate pins: `packages/core/tests/playbooks.test.ts:55-64`; ADR-0026 ordinary-loop decision: `cocoder/decisions/0026-onboard-existing-as-oscar-priority.md:47-61`. |
| Delete daemon `createDaemonPlaybookPhaseAction` wholesale without re-wiring audit tooling. | Production invocations of P1/P2/P3/P4/P5/P6 actions, deep-read dispatch, top-tier resolver, and event recording disappear. | Composition: `packages/daemon/src/launcher.ts:306-385`; P2/P3 load deep-read and dispatch via `dispatchPlay`: `packages/daemon/src/launcher.ts:313-343`; event emitters: `packages/daemon/src/launcher.ts:328-353`. |
| Remove P7 takeover hook without adding new ratification/apply caller. | `applyP6Governance` remains useful, but no production code applies staged `proposed-cocoder/**` through `runCommitGate`. | Current P7 apply hook: `packages/daemon/src/launcher.ts:366-384`; apply implementation: `packages/core/src/playbooks/p6-apply.ts:82-122`; boundary enforcement: `packages/core/src/commit-gate/gate.ts:67-73`. |
| Delete typed gate state without replacing founder answer persistence. | P5 currently can only infer founder checkpoint from executor gate metadata; `p5-input.ts` explicitly says no P4 answer artifact exists. | P5 gate use: `packages/core/src/playbooks/p5-action.ts:29-40`, `packages/core/src/playbooks/p5-action.ts:69-72`; missing durable P4 answers note: `packages/core/src/playbooks/p5-input.ts:16-23`. |
| Rename `takeover` mode without updating daemon summary type and UI payload tests. | `GET /workspaces/:id/priorities` returns `onboarding` summaries with old mode. | Summary type/map: `packages/daemon/src/priority-order.ts:15-31`; route response: `packages/daemon/src/routes.ts:354-359`; read-surface test: `packages/daemon/tests/read-surfaces.test.ts:183-185`. |
| Keep audit boundary but rename only some labels. | Boundary tests and daemon hooks assert `cocoder-takeover` label/message; partial rename breaks tests or preserves stale UI/audit events. | Core tests: `packages/core/tests/commit-gate.test.ts:178-229`; daemon hook labels/messages: `packages/daemon/src/launcher.ts:379-381`, `packages/daemon/src/launcher.ts:888`; boundary class itself is label-agnostic: `packages/core/src/commit-gate/gate.ts:41-57`. |
| Remove executor tests without adding Oscar-driven flow proof. | Test coverage loses proof that audit tools still execute in sequence: P1 artifacts, P2 fanout, P3 convergence, P4 questions, P5 staging, P6 apply. | Current E2E coverage: `packages/daemon/tests/mutations.test.ts:665-935`; pure module coverage exists but does not prove the new driver. |

## Working Disposition

Complete for analysis. The rebuild should delete the RETIRE set, keep the core audit engines/helpers, replace executor phase/gate wrappers with Oscar-priority invocation surfaces, and apply the rename in one lockstep change with loader/read-surface/test updates.
