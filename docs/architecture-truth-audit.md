# ARCHITECTURE.md Truth Audit

Generated: 2026-06-27

Scope: inventory only. `ARCHITECTURE.md` was read in full and not edited. Evidence below is from the current
workspace files and symbols. Verdicts mean:

- ACCURATE: the claim matches the checked implementation or repository shape.
- STALE: the claim describes an older or incomplete shape, but the intended concept still exists.
- WRONG: the claim conflicts with the checked implementation or path.
- UNVERIFIABLE: the claim is policy, history, or future behavior not provable from current code alone.

## Discrepancy Inventory

| # | ARCHITECTURE.md location | Claim | Evidence checked | Verdict | Proposed fix direction |
|---|---|---|---|---|---|
| 1 | lines 3-4 | Status is live v2; last-verified note references ADR-0031, ADR-0023, ADR-0029. | `cocoder/decisions/0031-architecture-reading-contract.md`; `cocoder/decisions/0023-workspace-commit-spine.md`; `cocoder/decisions/0029-founder-trusted-pre-run-snapshot.md`. | ACCURATE | - |
| 2 | lines 6-7 | Canonical vocabulary is `docs/glossary.md`; onboarded workspaces ship `cocoder/glossary.md`. | `docs/glossary.md`; `templates/workspace-cocoder/cocoder/glossary.md`; `packages/core/tests/scaffold.test.ts:63-66`. | ACCURATE | - |
| 3 | lines 11-19 | Current topology read is ADR-0008 plus ADR-0012, 0019, 0027, 0030, 0032; standalone `spikes/` and base `playbooks/` genres retired. | All named ADR files exist under `cocoder/decisions/`; `packages/personas/base/plays/` exists; no `packages/personas/base/playbooks/` directory; `basePlaybooksDir()` export removed from `packages/personas/src/index.ts` (run_268). | RESOLVED (code fixed) | Founder decision (run_267): dead `basePlaybooksDir()` export removed; zero callers repo-wide. |
| 4 | lines 21-47 | Diagram: install repo has `packages/core`, `packages/daemon`, `packages/ui`, `packages/personas/base`, and dogfood `cocoder/`; install-local has DB/runs/worktrees/workspace/settings/secrets/audit; dashboard talks to daemon; daemon to core. | Package directories exist; `local/cocoder.db`, `local/runs`, `local/worktrees`, `local/workspace`, `local/settings.json`, `local/secrets`, `local/oz-audit.log` exist; `packages/daemon/src/server.ts:158-167`; `packages/ui/src/main/daemon-client.ts:1-5`. | ACCURATE | - |
| 5 | lines 49-56 | Three storage zones: install public, install private `<CoCoder>/local/`, workspace tracked `<primary-root>/cocoder/`; no per-workspace local zone. | Root `.gitignore:1-8`; `packages/daemon/src/registry.ts:53-58`; `packages/core/src/store/portable/paths.ts:19-26`; template has `templates/workspace-cocoder/cocoder/` and no template-local zone. | ACCURATE | - |
| 6 | lines 58-65 | CoCoder repo is both engine install and dogfood workspace host; dogfood `cocoder/decisions/` is the live CoCoder ADR tree. | `cocoder/AGENTS.md`; `cocoder/decisions/README.md`; current repo has `packages/` and tracked `cocoder/`. | ACCURATE | - |
| 7 | lines 67-69 | `local/` lives inside the CoCoder folder and is synced outside git. | `local/` exists; `.gitignore:5-8` ignores all of it except `local/README.md`; `local/README.md`. | ACCURATE | - |
| 8 | lines 73-85 | Commit spine is one core service; `commitFiles`/`commitScoped` return one receipt with commit sha, changed files, out-of-lane flags, and errors. | `packages/core/src/commit-gate/workspace-commit.ts:1-16`; `packages/core/src/commit-gate/workspace-commit.ts:29-40`; `packages/core/src/commit-gate/workspace-commit.ts:60-97`. | ACCURATE | - |
| 9 | lines 73-77 | No actor reimplements `git commit`; older divergent paths `runCommitGate`, `commitGovernance`, `gateCommitRepair` were replaced. | `packages/core/src/commit-gate/gate.ts:57-102` still exports `runCommitGate`; `packages/daemon/src/launcher.ts:670-676` still exports `commitGovernance`; `rg gateCommitRepair` found ADR/docs history but no active exported implementation. Both live functions funnel through `commitFiles`. | RESOLVED (doc fixed) | Fixed doc: older paths now funnel through the spine, rather than saying the named paths are gone. |
| 10 | lines 79-85 | One direct mode: active checkout/branch, no worktree/run branch/landing; out-of-lane paths committed and flagged by default. | `packages/core/src/runner/runner.ts:441-447`; `packages/core/src/commit-gate/gate.ts:5-7`; `packages/core/src/commit-gate/gate.ts:75-101`. | ACCURATE | - |
| 11 | lines 87-100 | Launch snapshots founder/builder dirt and governance dirt before a run; `strictPreRunDirt` keeps the hard-stop path. | `packages/core/src/runner/runner.ts:460-519`; `packages/cli/src/run.ts:490-493`; `packages/daemon/src/routes.ts:89-95`. | ACCURATE | - |
| 12 | lines 102-107 | Atomic authoring Plays use the same spine for create/edit/archive-priority. | `packages/cli/src/run.ts:311-446`; `packages/daemon/src/launcher.ts:2217-2257` checked by symbol search; `packages/daemon/tests/authoring-play.test.ts`. | ACCURATE | - |
| 13 | lines 109-113 | Governance/docs are direct-to-branch with "light / none (can't break a build)"; product code is verified before commit; shared GitHub push is non-gating. | Direct commit paths exist in `packages/daemon/src/launcher.ts:670-676` and `packages/core/src/runner/runner.ts:1427-1438`; however governance/docs are tested by suites such as `packages/core/tests/orchestration-contracts.test.ts`, and the run standards require affected suites for docs/governance. | RESOLVED (doc fixed) | Fixed doc: removed "can't break a build"; verification is lighter/risk-matched but governed docs can be test-pinned. |
| 14 | lines 115-120 | Single-writer-per-workspace lock serializes writes; no `pending-landing`, held-back queue, or run branch path. | Daemon reserves `ctx.inFlight` per workspace in `packages/daemon/src/launcher.ts:825-830`; direct mode in `packages/core/src/runner/runner.ts:441-447`; no active code path named `pending-landing` found outside docs/history search hits. | ACCURATE | - |
| 15 | lines 124-132 | Run loop is owned by ADR-0013/0016/0017/0026; contract drill-down is `docs/orchestration-contract-ownership.md`; landing flows through commit spine. | All named ADR/doc files exist; runner owns loop in `packages/core/src/runner/runner.ts`; commit spine code cited above. | ACCURATE | - |
| 16 | lines 134-138 | Oscar delegates atoms to Bob, waits for Bob marker, verifies real diff/evidence, then rejects or commits. | `packages/core/src/runner/runner.ts:1330-1401`; `packages/core/src/runner/agent-step.ts`; `packages/core/src/commit-gate/gate.ts:57-102`. | ACCURATE | - |
| 17 | lines 140-146 | `detectRunnerImpact()` refuses self-impacting priorities unless `allowSelfImpacting`; `independent-of-runner` priorities are refused by normal daemon runner and use `cocoder run-independent`. | `packages/core/src/priorities/runner-impact.ts:3-44`; `packages/daemon/src/launcher.ts:877-914`; `packages/cli/src/run.ts:42-43`; `packages/cli/src/run.ts:496-557`. | ACCURATE | - |
| 18 | lines 148-151 | Observation is tiered by "direct your primary": Oscar directs Bob, Deb nudges Oscar, Oz directs through daemon tools. | Persona prompts under `packages/personas/base/`; Deb/Oz nudge/repair handlers in `packages/daemon/src/launcher.ts:1248-1276` and `packages/daemon/src/launcher.ts:1359-1391`; no single code enforcer for the whole policy found. | UNVERIFIABLE | Leave as policy, or add a pointer to the prompt/contract owner that enforces the tiering. |
| 19 | lines 153-157 | Deb has live status feed, Oscar-only nudge, and gate-enforced CoCoder repair mode; Deb does not rescue failed product runs. | `packages/daemon/src/launcher.ts:1359-1391`; `packages/daemon/src/oscar-deb-repair.ts`; `packages/daemon/tests/oscar-deb-repair.test.ts`; `packages/daemon/tests/oscar-deb-repair-op.test.ts`. | ACCURATE | - |
| 20 | lines 159-164 | Oz is idle control plane with launch/status/lifecycle/authoring/refresh/nudge/repair/read-governed; read-governed is default-allow with denylist, live from disk. | `packages/daemon/src/routes.ts`; `packages/daemon/src/launcher.ts:1922-1955`; `packages/daemon/src/launcher.ts:2067-2140`; `packages/daemon/src/launcher.ts:2509-2534`; `packages/core/src/write-scope/governed-read.ts:1-11`. | ACCURATE | - |
| 21 | lines 166-170 | Existing-repo onboarding and drift run as ordinary Oscar priorities; standalone phase-executor is retired. | Template seeds `onboard-existing` conditionally in `packages/core/src/scaffold/scaffold.ts:7-8` and `packages/core/src/scaffold/scaffold.ts:84-93`; drift engine exists at `packages/core/src/drift/`; `packages/core/src/playbooks/` now exports only `recon.ts` (`inventoryRepo`) for drift/read-reality tooling (run_268). | RESOLVED (code fixed) | Founder decision (run_267): retired v1 P1–P6 playbook modules deleted; sole live symbol `inventoryRepo` kept. |
| 22 | lines 172-189 | Git ignore matrix: `/local/` ignored except README; `cocoder/` tracked; env/secrets ignored; examples tracked. | Root `.gitignore:1-22`; `templates/install-local/secrets/.gitignore`; `git status --short` shows tracked/untracked governance dirt, not ignored `cocoder/`. | ACCURATE | - |
| 23 | lines 191-197 | Pattern: example files are tracked; bootstrap copies `templates/workspace-cocoder/` to `<primary-root>/cocoder/`; `local/` survives git updates. | `templates/install-local/*.example.*`; `packages/core/src/scaffold/scaffold.ts:75-95`; `.gitignore:5-8`. | ACCURATE | - |
| 24 | lines 202-215 | Directory layout lists seven TypeScript packages and names `core`, `personas`, `adapters`, `session-hosts`, `daemon`, `cli`, `ui`. | `find packages -maxdepth 1 -type d` shows those seven directories; each has `package.json`. | ACCURATE | - |
| 25 | lines 207-215 | Package responsibilities: core engine; personas base; adapters CLI drivers; session-hosts cmux; daemon Oz; cli binary; ui Electron. | `packages/core/src/runner/`; `packages/personas/base/`; `packages/adapters/src/{claude,codex,cursor-agent}.ts`; `packages/session-hosts/src/cmux/driver.ts`; `packages/daemon/bin/oz.mjs`; `packages/cli/bin/cocoder.mjs`; `packages/ui/package.json`. | ACCURATE | - |
| 26 | lines 208-210 | Base Plays live in `packages/personas/base/plays/`; core loader merges repo deltas with `loadEffectivePlay`. | `packages/personas/base/plays/*.md`; `packages/core/src/plays/effective.ts:48-55`; `packages/personas/src/index.ts:8-10`. | ACCURATE | - |
| 27 | line 217 | `examples/` exists for example custom personas. | `examples/` directory exists, but the file inventory at max depth 3 did not show example files in the first scan. | UNVERIFIABLE | If examples are required, add/keep actual example files or reword as a reserved directory. |
| 28 | lines 222-236 | Dogfood `cocoder/` governance shape includes AGENTS, PLAYBOOK, SESSION_LOG, failure catalog, decisions, priorities, tickets, personas, plays, memory, standards, zArchive. | Those paths exist. Current tree also includes tracked `cocoder/audit/` and `cocoder/runs/`, confirmed by `git ls-files cocoder/audit cocoder/runs`. | RESOLVED (doc fixed) | Fixed doc: added `audit/` and `runs/` layout entries with role notes. |
| 29 | line 228 | Decisions are "0001-0019+". | Current decisions run through `cocoder/decisions/0044-machine-local-cache-retention.md`. | RESOLVED (doc fixed) | Fixed doc: decisions now say `0001 through 0044, and growing`. |
| 30 | lines 237-243 | Local zone has `cocoder.db`, `runs/`, historical `worktrees/`, `workspace/`, legacy `workspaces.json`, settings, secrets, audit, scratch, and tracked README. | `local/cocoder.db`; `local/runs/`; `local/worktrees/`; `local/workspace/`; `local/workspaces.json`; `local/settings.json`; `local/secrets/`; `local/oz-audit.log`; `local/scratch/`; `local/README.md`. | ACCURATE | - |
| 31 | lines 245-251 | Any managed repo gets an identical `cocoder/` shape including decisions, priorities, tickets, memory, standards, personas deltas/custom/assignments, plays/deltas, and no `local/`. | `templates/workspace-cocoder/cocoder/` lacks `plays/`, `plays/deltas/`, `personas/deltas/`, `tickets/open/`, and `tickets/closed/`; `packages/core/tests/scaffold.test.ts:40-57` pins the thinner template output. | RESOLVED (doc fixed) | Fixed doc: distinguish the full dogfood shape from the thinner starter template. |
| 32 | lines 258-262 | Persona boundaries for Oz/Oscar/Bob/Deb/Quinn. | Base persona files exist: `packages/personas/base/oz.md`, `oscar.md`, `bob.md`, `deb.md`, `quinn.md`; daemon exposes Oz/Deb operations; runner delegates Oscar/Bob. | ACCURATE | - |
| 33 | lines 264-268 | Ian and Phil are custom-persona patterns only; live base set is five; testing is Play capability through `write-tests`/`run-tests`. | No `packages/personas/base/ian.md` or `phil.md`; `docs/custom-personas.md`; `packages/personas/base/plays/write-tests.md`; `packages/personas/base/plays/run-tests.md`; `cocoder/decisions/0033-testing-as-a-play-capability.md`. | ACCURATE | - |
| 34 | lines 272-278 | Play taxonomy/schema metadata fields live in `packages/core/src/plays/types.ts`. | `packages/core/src/plays/types.ts:5-12`; `packages/core/src/plays/types.ts:29-55`; `packages/core/src/plays/types.ts:57-83`. | ACCURATE | - |
| 35 | lines 280-285 | Prompt-only and hybrid Play execution is owned by `packages/core/src/plays/dispatch.ts`; deterministic refs are repo-relative, `.mjs` uses Node, non-zero gates LLM, output feeds prompt. | `packages/core/src/plays/dispatch.ts:121-155`; `packages/core/src/plays/dispatch.ts:157-215`. | ACCURATE | - |
| 36 | lines 287-290 | Launch prompts receive compact per-persona Play manifests; bodies lazy-load at dispatch. | `packages/core/src/plays/manifest.ts:9-25`; `packages/core/src/runner/prompts.ts`; `packages/core/src/plays/dispatch.ts:211-215`. | ACCURATE | - |
| 37 | lines 292-299 | Optional Play handoff validation is `packages/core/src/plays/request.ts`; assignment is daemon/runner plumbing; mandatory triggers are in `plays/triggers.ts` and runner wrap-up dispatch. | `packages/core/src/plays/request.ts:40-87`; `packages/core/src/plays/triggers.ts:4-20`; `packages/core/src/runner/runner.ts:1402-1443`; `packages/daemon/src/launcher.ts:2234-2257`. | ACCURATE | - |
| 38 | lines 301-306 | Production output selector is exported `validatePlayOutput` in `packages/core/src/runner/runner.ts`. | Actual implementation is `packages/core/src/plays/founder-closeout.ts:403-408`; `packages/core/src/index.ts:118-131` re-exports it; `packages/core/src/runner/runner.ts:29` imports it and uses it at `runner.ts:1442`. | RESOLVED (doc fixed) | Fixed doc: point the owner to `packages/core/src/plays/founder-closeout.ts`, with runner as a consumer. |
| 39 | lines 308-314 | Plays are one-level procedures; no recursive Play delegation, no `PlayAssignment[]`, no full-body injection into every prompt; proof is `scripts/proof-hybrid-play.mjs`. | `packages/core/src/plays/dispatch.ts:1-9`; `packages/core/src/plays/dispatch.ts:56-70` uses one `PlayAssignment`; `scripts/proof-hybrid-play.mjs` exists and calls `validatePlayOutput`; archive path `cocoder/priorities/archive/play-dispatch-boundary.md` exists. | ACCURATE | - |
| 40 | lines 318-324 | Deb and Oz are adjacent observation tiers; daemon keeps both on `packages/core` ports instead of reimplementing orchestration. | Deb/Oz handlers in `packages/daemon/src/launcher.ts`; core ports/types in `packages/core/src/session-host/types.ts`, `packages/core/src/store/types.ts`, and `packages/core/src/runner/runner.ts`. | ACCURATE | - |
| 41 | lines 328-332 | "The six packages already exist under `packages/`"; dependency rule says core has no workspace deps; adapters/session-hosts/ui depend only on core; daemon/cli depend on core + adapters + session-hosts. | Actual packages are seven: `adapters`, `cli`, `core`, `daemon`, `personas`, `session-hosts`, `ui`; `packages/daemon/package.json` and `packages/cli/package.json` also depend on `@cocoder/personas`; `packages/personas/package.json` has no workspace deps. | RESOLVED (doc fixed) | Fixed doc: say seven packages, add `personas` to the topology rule, and update daemon/cli dependency description. |
| 42 | line 334 | Dependency rule is enforced by `node scripts/check-topology.mjs`. | `scripts/check-topology.mjs`; root `package.json` script `check:topology`. | ACCURATE | - |
| 43 | lines 338-340 | TypeScript across packages; packages export `./src/index.ts`; no `.mjs` orchestration core; no external validation library; pnpm workspaces and Node from `.nvmrc`. | All seven `packages/*/package.json` export `./src/index.ts`; root `package.json` has `packageManager: pnpm@10.30.3`; `.nvmrc` is `25.1.0`; package manifests contain no zod/yup/joi/ajv/valibot dependencies. | ACCURATE | - |
| 44 | lines 344-348 | Cmux terminal host creates one cmux workspace per run and split panes for personas; operations address `--workspace` and `--surface`; driver opens cmux if socket unavailable. | `packages/session-hosts/src/cmux/driver.ts:73-74`; `driver.ts:100-115`; `driver.ts:130-140`; `driver.ts:160-162`; `driver.ts:215-242`; `driver.ts:267-284`; `driver.ts:60-62`. | ACCURATE | - |
| 45 | lines 352-359 | `local/workspaces.json` registers workspaces; entries store paths under `${COCODER_HOME}` or named roots from `local/roots.yaml`; absolute paths only when neither token applies and warning is logged. | Current owner is `local/workspace/*.code-workspace`: `packages/daemon/src/registry.ts:1-4`, `registry.ts:53-58`, `registry.ts:116-170`; it falls back to legacy `local/workspaces.json` at `registry.ts:137-170`; variable expansion supports `${NAME}` from vars/env at `registry.ts:39-50`; no `local/roots.yaml` reader or warning path found. | RESOLVED (doc fixed) | Fixed doc: describe `.code-workspace` registry and `${COCODER_HOME}`/env expansion; remove named-root/roots.yaml behavior. |
| 46 | lines 363-365 | Oz HTTP daemon binds `127.0.0.1` only. | `packages/daemon/src/server.ts:246`; `packages/daemon/src/server.ts:261`; `packages/daemon/src/security.ts:12-14`. | ACCURATE | - |
| 47 | line 366 | State-changing endpoints require per-install token at `local/secrets/oz-token`. | `packages/daemon/src/secrets.ts:1-25`; `packages/daemon/src/server.ts:147-148`; `packages/daemon/src/server.ts:218-222`. | ACCURATE | - |
| 48 | lines 367-368 | Host/Origin DNS rebinding checks and CSRF on POST/DELETE. | `packages/daemon/src/security.ts:41-58`; `packages/daemon/src/security.ts:70-76`; `packages/daemon/src/server.ts:201-222`; `packages/daemon/tests/security.test.ts:18-36`. | ACCURATE | - |
| 49 | line 369 | Settings endpoints never return secret values, only references like `"openai": "ref:env:OPENAI_API_KEY"`. | `packages/daemon/src/settings.ts:5-17` settings contain poll/default workspace/auto compact/retention only; route search found `/settings` but no secret reference fields. | RESOLVED (doc fixed) | Fixed doc: current settings surface has no secret-bearing fields; reference-redaction is a future invariant only if a secret settings endpoint is added. |
| 50 | line 370 | All launch/stop actions write to `local/audit/oz-actions.jsonl` with timestamp, persona, workspace, run id, outcome. | Actual audit helper writes `local/oz-audit.log` in `packages/daemon/src/audit.ts:7-17`; launch appends in `packages/daemon/src/launcher.ts:440-443`; teardown appends in `launcher.ts:1189-1191`. | RESOLVED (doc fixed) | Fixed doc to `local/oz-audit.log`. |
| 51 | line 371 | No shell-string interpolation of workspace paths; argv arrays only. | Runtime spawns often use argv arrays (`packages/daemon/src/launcher.ts:558-563`, `packages/session-hosts/src/cmux/driver.ts:287-290`), but handoff/display commands use shell-quoted strings (`packages/daemon/src/launcher.ts:486-490`, `launcher.ts:586-588`) and cmux launch scripts shell-quote args (`packages/session-hosts/src/cmux/launch.ts:15-18`). | RESOLVED (doc fixed) | Fixed doc: process-spawning paths use argv arrays; display/handoff shell strings are allowed when quoted and non-executed by the daemon. |
| 52a | lines 375-382 | Oz improvement routing uses categories `cocoder-product`, `workspace-shared`, `install-local`, `upstream-candidate`. | Routing guide exists at `docs/oz-improvement-routing.md:40-57`; shared standards points to it at `packages/personas/base/shared-standards.md:96`. | RESOLVED (doc fixed) | Fixed doc: points classification at the Routing Guide and keeps the current four target zones. |
| 52b | line 382 | CoCoder product improvements require dogfood workspace and developer mode. | No current code symbol for `developer-mode` exists in live `packages/`; v1's `--developer-mode` deny-gate was dropped in the v2 rebuild (archived under `cocoder/zArchive/source/`); v2 gates product writes via per-run write-scope + commit-gate hold-back (`packages/core/src/runner/prompts.ts`, `packages/core/src/runner/triage.ts`). | RESOLVED (doc fixed) | Founder decision (run_267, Option A): corrected ARCHITECTURE.md:390 and docs/oz-improvement-routing.md:44 to v2 write-scope + commit-gate reality and removed the stale "developer mode enabled" phrasing. |
| 53 | lines 386-392 | Reference paths: UI design ref, maintained UI, design spec, Oz design brief, Play system owners, drift audit owners, ADR index, NOTICE, dogfood AGENTS, PLAYBOOK/priorities. | Existing: `packages/ui/design-ref/`, `packages/ui/src/`, `packages/ui/src/renderer/styles/design-spec.md`, `docs/oz-design-brief.md`, `packages/core/src/plays/types.ts`, `packages/core/src/plays/dispatch.ts`, `scripts/proof-hybrid-play.mjs`, `packages/core/src/drift/`, `docs/drift-audit-ownermap.md`, `scripts/proof-drift-audit.mjs`, `cocoder/decisions/README.md`, `NOTICE`, `cocoder/AGENTS.md`, `cocoder/PLAYBOOK.md`, `cocoder/priorities/`. | ACCURATE | - |

## Resolved Follow-Ups

The wrong-claim follow-ups from the initial audit are marked `RESOLVED (doc fixed)` in rows 31, 38, 41,
45, and 50. Founder code-or-doc calls from run_267 are marked `RESOLVED (code fixed)` in rows 3 and 21
(run_268). Remaining `UNVERIFIABLE` rows are unchanged and still need separate scoping.

## ADR Reference Audit

Coverage summary: 43 ADR/index files scanned; 291 references checked (282 Markdown links plus 9 targeted
path/symbol checks for current-code claims); 9 discrepancies found.

| # | ADR + location | Claim/reference | Evidence checked | Verdict | Proposed fix direction |
|---|---|---|---|---|---|
| ADR-1 | `cocoder/decisions/0023-workspace-commit-spine.md:4` | Links `orchestration-operating-model-reset` to `../priorities/orchestration-operating-model-reset.md`. | `test -e cocoder/priorities/orchestration-operating-model-reset.md` failed; `find cocoder -name '*orchestration-operating-model-reset*'` found `cocoder/zArchive/priorities/v2/orchestration-operating-model-reset.md`. | STALE | Point the link at the archived priority or reword as historical build context without a live priority link. |
| ADR-2 | `cocoder/decisions/0024-governance-pre-run-snapshot.md:6` | Links `governance-authoring-plays` to `../priorities/governance-authoring-plays.md`. | `test -e cocoder/priorities/governance-authoring-plays.md` failed; `find cocoder/priorities -name '*governance-authoring-plays*'` found `cocoder/priorities/archive/governance-authoring-plays.md`. | STALE | Update the link to `../priorities/archive/governance-authoring-plays.md` if the reference is historical. |
| ADR-3 | `cocoder/decisions/0025-atomic-authoring-plays.md:4` | Links `governance-authoring-plays` to `../priorities/governance-authoring-plays.md`. | `test -e cocoder/priorities/governance-authoring-plays.md` failed; archived file exists at `cocoder/priorities/archive/governance-authoring-plays.md`. | STALE | Update the link to the archived priority path. |
| ADR-4 | `cocoder/decisions/0025-atomic-authoring-plays.md:11` | Builds on `0013-multi-atom-delegation.md`. | `test -e cocoder/decisions/0013-multi-atom-delegation.md` failed; current ADR is `cocoder/decisions/0013-orchestration-observation.md`. | WRONG | Replace the filename with `./0013-orchestration-observation.md`. |
| ADR-5 | `cocoder/decisions/0025-atomic-authoring-plays.md:13` | Builds on `0010-objective-first.md`. | `test -e cocoder/decisions/0010-objective-first.md` failed; current ADR is `cocoder/decisions/0010-taxonomy-and-authoring.md`. | WRONG | Replace the filename with `./0010-taxonomy-and-authoring.md`. |
| ADR-6 | `cocoder/decisions/0027-workspace-storage-contract.md:11` | Links `workspace-segmentation` owner map to `../priorities/workspace-segmentation.md`. | `test -e cocoder/priorities/workspace-segmentation.md` failed; `find cocoder/priorities -name '*workspace-segmentation*'` found `cocoder/priorities/archive/workspace-segmentation.md` and `cocoder/priorities/archive/workspace-segmentation.owner-map.md`. | STALE | Point to the archived owner map or archived priority, whichever owns the referenced evidence. |
| ADR-7 | `cocoder/decisions/0028-play-taxonomy-three-axes.md:4` | Links `orchestration-audit-and-refactor` to `../priorities/orchestration-audit-and-refactor.md`. | `test -e cocoder/priorities/orchestration-audit-and-refactor.md` failed; archived file exists at `cocoder/priorities/archive/orchestration-audit-and-refactor.md`. | STALE | Update the link to `../priorities/archive/orchestration-audit-and-refactor.md`. |
| ADR-8 | `cocoder/decisions/0020-primary-root-audit.md:131` | Amendment says the existing-repo seeded priority is `packages/personas/base/priorities/onboard-existing.md`. | `ls packages/personas/base/priorities/onboard-existing.md` failed; `templates/workspace-cocoder/cocoder/priorities/onboard-existing.md` exists; `packages/core/tests/scaffold.test.ts:75-91` pins conditional scaffold seeding. | STALE | Update the amendment to point at the template-seeded priority path. |
| ADR-9 | `cocoder/decisions/0020-addendum-phase-executor.md:28-33,113-118` | "Current code" inventory says onboarding Playbooks live under `packages/personas/base/playbooks/`, `packages/core/src/playbooks/loader.ts` owns executable metadata, and `packages/core/tests/playbooks.test.ts` pins the skeletons. | `ls packages/personas/base/playbooks packages/core/src/playbooks/loader.ts packages/core/tests/playbooks.test.ts` failed; `ls packages/core/src/playbooks` shows retained phase/tooling modules such as `recon.ts`, `p2-fanout.ts`, and `p6-apply.ts`; `cocoder/decisions/0020-addendum-phase-executor.md:3-12` marks the executor mode superseded. | NO EDIT (historical) | Left as-is: the whole addendum is loudly self-marked SUPERSEDED and "retained as the historical design" at lines 3-10, so the dated code inventory is a historical snapshot, not a current-behavior claim. Editing it would rewrite a dated record. |

### Resolution (run_267, Oscar support edit)

ADR-1 through ADR-8 — **RESOLVED (doc fixed)**: all eight broken/stale references corrected to their real
targets, each verified to resolve in the current tree:
- ADR-1 `0023:4` → `../zArchive/priorities/v2/orchestration-operating-model-reset.md`
- ADR-2 `0024:6` → `../priorities/archive/governance-authoring-plays.md`
- ADR-3 `0025:4` → `../priorities/archive/governance-authoring-plays.md`
- ADR-4 `0025:11` → `./0013-orchestration-observation.md`
- ADR-5 `0025:13` → `./0010-taxonomy-and-authoring.md`
- ADR-6 `0027:11` → `../priorities/archive/workspace-segmentation.owner-map.md`
- ADR-7 `0028:4` → `../priorities/archive/orchestration-audit-and-refactor.md`
- ADR-8 `0020:131` → `templates/workspace-cocoder/cocoder/priorities/onboard-existing.md`

ADR-9 — **NO EDIT (historical)**, rationale in the row above.

## Phase 2 — remaining doc surface (run_268 wrap)

Code follow-ups from run_267 are closed (rows 3, 21, 52b). Resume with these surfaces in order (no
founder input required for the sweep itself):

1. **cocoder/ governance docs** — PLAYBOOK, AGENTS, glossary, failure-catalog, personas/, standards/, plays/ (Oscar-lane; builder cannot write these).
2. **Root README and CONTRIBUTING** — stale CI/contributor claims; open ticket [0037](../cocoder/tickets/open/0037-contributing-pr-template-stale-rg-ci-gate.md) overlaps CONTRIBUTING.
3. **Design-intent briefs under docs/** — oz-design-brief, oz-streaming-design, ui-dev-notes, research/; path-ref scan only (do not flag aspirational design as wrong).
4. **Cleanup (when priority completes):** whether `docs/architecture-truth-audit.md` and `docs/docs-files-truth-audit.md` should be archived rather than kept permanently in `docs/`.

**Founder code-or-doc calls — resolution (run_268):**

| Item | Verdict |
|---|---|
| `basePlaybooksDir()` export (row 3) | **RESOLVED (code fixed, run_268):** dead export removed from `packages/personas/src/index.ts`; zero callers. |
| `packages/core/src/playbooks/` p1–p6 modules (row 21) | **RESOLVED (code fixed, run_268):** 23 dead modules + 11 orphaned tests deleted; `recon.ts` (`inventoryRepo`) kept as sole live export. |
| Developer-mode routing gate (row 52b) | **RESOLVED (doc fixed, run_267):** ARCHITECTURE.md and oz-improvement-routing corrected to v2 write-scope + commit-gate reality. |
