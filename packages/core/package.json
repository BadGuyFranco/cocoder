{
  "name": "core",
  "version": "0.1.0",
  "type": "module",
  "main": "cli.mjs",
  "scripts": {
    "pretest": "pnpm --filter schemas build",
    "build": "pnpm --filter schemas build && node cli.mjs validate-contracts",
    "lint": "node scripts/check-mjs.mjs",
    "test": "node scripts/test.mjs",
    "typecheck": "node --check cli.mjs"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "yaml": "^2.7.1"
  }
}
