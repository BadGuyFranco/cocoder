{"run":{"id":"run_21","workspaceId":"cocoder","priorityId":"plays-mechanism","status":"completed","createdAt":1780153227239,"endedAt":1780158783896},"sessions":[{"id":"ses_6f3cf4b15d964781","runId":"run_21","persona":"oscar","sessionRef":"surface:2","startedAt":1780153232727,"exitCode":null,"deepLinkable":false},{"id":"ses_f8b8a70ef70946be","runId":"run_21","persona":"bob","sessionRef":"surface:3","startedAt":1780153232935,"exitCode":null,"deepLinkable":false},{"id":"ses_26c9fa38b5314f24","runId":"run_21","persona":"deb","sessionRef":"surface:4","startedAt":1780153233317,"exitCode":null,"deepLinkable":false}],"workItems":[{"id":"wi_fbc6fd5652254896","runId":"run_21","sourcePersona":"oscar","targetPersona":"bob","task":"Make the wrap-up path FAIL LOUD on a stale daemon instead of silently faking a closeout. CONTEXT: a long-lived Oz daemon serves the orchestration code it booted with; when repo HEAD advances past the daemon's bootSha, the daemon is 'stale' and runs old code. Today the daemon DETECTS this (packages/daemon/src/launcher.ts warnIfDaemonStale -> records a 'daemon-stale' event, console.warns, audits) but is observability-only and 'never blocks a launch'. The failure we are fixing: on a stale daemon the boot-time launcher does NOT pass wrapPlay/wrapPlayAssignment into RunInput, so the runner's wrapup branch (packages/core/src/runner/runner.ts ~line 306) silently takes the OLD inline fallback (pickup = directive.pickup) and the run ends status 'completed' as if it were a real closeout. This has silently burned three runs (19/20/21). NOTE the fix takes effect on the NEXT daemon restart, not the current process — that is expected; do not try to affect the running daemon.\n\nDESIGN (founder-approved): keep 'launch never blocks' — the daemon must still launch the run. Only the WRAP-UP must refuse to silently degrade when the daemon is stale.\n\nCHANGES:\n1. packages/core/src/runner/runner.ts + its RunInput type: add a new OPTIONAL field `daemonStale?: boolean` to RunInput (default undefined/false). In the `if (directive.kind === 'wrapup')` branch, BEFORE the existing `if (input.wrapPlay && input.wrapPlayAssignment)` check, add: if `input.daemonStale` is true, FAIL LOUD — do NOT dispatch the Play, do NOT take the silent old-path fallback, do NOT mark the run 'completed' with a normal-looking pickup. Instead: record a distinct event (type 'wrapup-stale-abort', data {atoms: n, ...}); set the run's pickup to an unmistakable founder-facing warning (e.g. begins '⚠️ STALE DAEMON — wrap-up did NOT run on current code. NO valid closeout/proof was produced. Restart the daemon (scripts/oz.sh restart) and re-run this priority.'); and ensure the run's FINAL status is 'failed' (RunStatus already supports 'failed'), NOT 'completed'. Do this WITHOUT routing through the existing `fail()` helper (that triggers Deb triage + throws, which is wrong here — staleness is operational, not a code fault to triage). Keep it a clean break out of the loop that then lands on a 'failed' status at run-end. The existing fresh-daemon behavior (wrapPlay present -> dispatchPlay on assigned model -> commit-gate -> pickup from Play output) and the legitimate not-stale no-Play fallback must remain BYTE-FOR-BYTE unchanged.\n2. packages/daemon/src/launcher.ts: in launchRun, after `headNow` is computed, set the new field on the RunInput before runRun, computing staleness the SAME way warnIfDaemonStale does: stale = ctx.bootSha !== 'unknown' && headNow !== 'unknown' && headNow !== ctx.bootSha. Leave warnIfDaemonStale and its 'daemon-stale' event/warn/audit exactly as-is (still want the observability). The CLI standalone path (packages/cli/src/run.ts) must NOT set daemonStale (a fresh CLI process is never stale) — leave run.ts unchanged so the field stays absent there.\n\nACCEPTANCE CRITERIA (prove with hermetic unit tests in packages/core/tests/runner.test.ts):\n(a) daemonStale=true + a wrapup directive -> NO Play dispatched, run-end status is 'failed', a 'wrapup-stale-abort' event is recorded, and pickup is the warning string (NOT directive.pickup used as a normal closeout). \n(b) daemonStale=false (or absent) + wrapPlay present -> existing Play-dispatch path is unchanged (Play runs on assigned adapter, commit-gate against Play scope, pickup from Play output).\n(c) daemonStale=false + no wrapPlay -> existing legitimate old-path fallback unchanged (pickup = directive.pickup, status completed).\n\nMUST NOT BREAK: full `pnpm -r test` green, `pnpm typecheck` clean, `pnpm check:topology` clean (runner stays core-only; daemon may import core). Do NOT touch docs, ADRs, priorities, or assignments.json in this atom. Do NOT run git/commit — the CoCoder runner commits your in-scope diff after I verify. WRITE-SCOPE: packages/core/** and packages/daemon/** only.","writeScope":["packages/**"],"status":"done","createdAt":1780158568123}],"commitLinks":[{"id":"cl_15c96b55644c49d1","runId":"run_21","workItemId":"wi_fbc6fd5652254896","commitSha":"9b04a09291df92106e1fc2369be0d44ff5571673","message":"plays-mechanism: atom 0 via CoCoder run run_21","files":["packages/core/src/runner/runner.ts","packages/core/tests/runner.test.ts","packages/daemon/src/launcher.ts"],"createdAt":1780158719707}],"events":[{"id":"ev_36ec71f2729b40ef","runId":"run_21","type":"daemon-stale","data":{"bootSha":"bf19eb7b605a288e59b5009f2468940f61bc06c0","headSha":"a9af2da8f2edbbb50b000dfd12006351677f418d"},"at":1780153227240},{"id":"ev_58ef66a0eb164ba8","runId":"run_21","type":"run-start","data":{"priority":"plays-mechanism","runDir":"/Volumes/NAS LOCAL/CoCoder/local/runs/run_21"},"at":1780153227240},{"id":"ev_703f482525544052","runId":"run_21","type":"preflight","data":{"persona":"oscar","cli":"claude","ok":true,"checks":[{"name":"installed","ok":true,"detail":"2.1.158 (Claude Code)"},{"name":"authenticated","ok":true,"detail":"logged in (claude auth status)"},{"name":"model","ok":true,"detail":"(claude default)"}]},"at":1780153227450},{"id":"ev_8745b9bf01114b52","runId":"run_21","type":"preflight","data":{"persona":"bob","cli":"codex","ok":true,"checks":[{"name":"installed","ok":true,"detail":"codex-cli 0.135.0"},{"name":"authenticated","ok":true,"detail":"Logged in using ChatGPT"},{"name":"model","ok":true,"detail":"(codex default)"}]},"at":1780153227564},{"id":"ev_50e6bf6197484634","runId":"run_21","type":"spawn","data":{"persona":"oscar","ref":"surface:2"},"at":1780153232727},{"id":"ev_b35381cde85845f5","runId":"run_21","type":"spawn","data":{"persona":"bob","ref":"surface:3"},"at":1780153232937},{"id":"ev_2dc37ab8fb8c4521","runId":"run_21","type":"preflight","data":{"persona":"deb","cli":"codex","ok":true,"checks":[{"name":"installed","ok":true,"detail":"codex-cli 0.135.0"},{"name":"authenticated","ok":true,"detail":"Logged in using ChatGPT"},{"name":"model","ok":true,"detail":"(codex default)"}]},"at":1780153233057},{"id":"ev_edfcf528bc0d4d79","runId":"run_21","type":"spawn","data":{"persona":"deb","ref":"surface:4"},"at":1780153233317},{"id":"ev_2568c5bbed7246d1","runId":"run_21","type":"delegation","data":{"workItemId":"wi_fbc6fd5652254896","atom":0,"task":"Make the wrap-up path FAIL LOUD on a stale daemon instead of silently faking a closeout. CONTEXT: a long-lived Oz daemon serves the orchestration code it booted with; when repo HEAD advances past the daemon's bootSha, the daemon is 'stale' and runs old code. Today the daemon DETECTS this (packages/daemon/src/launcher.ts warnIfDaemonStale -> records a 'daemon-stale' event, console.warns, audits) but is observability-only and 'never blocks a launch'. The failure we are fixing: on a stale daemon the boot-time launcher does NOT pass wrapPlay/wrapPlayAssignment into RunInput, so the runner's wrapup branch (packages/core/src/runner/runner.ts ~line 306) silently takes the OLD inline fallback (pickup = directive.pickup) and the run ends status 'completed' as if it were a real closeout. This has silently burned three runs (19/20/21). NOTE the fix takes effect on the NEXT daemon restart, not the current process — that is expected; do not try to affect the running daemon.\n\nDESIGN (founder-approved): keep 'launch never blocks' — the daemon must still launch the run. Only the WRAP-UP must refuse to silently degrade when the daemon is stale.\n\nCHANGES:\n1. packages/core/src/runner/runner.ts + its RunInput type: add a new OPTIONAL field `daemonStale?: boolean` to RunInput (default undefined/false). In the `if (directive.kind === 'wrapup')` branch, BEFORE the existing `if (input.wrapPlay && input.wrapPlayAssignment)` check, add: if `input.daemonStale` is true, FAIL LOUD — do NOT dispatch the Play, do NOT take the silent old-path fallback, do NOT mark the run 'completed' with a normal-looking pickup. Instead: record a distinct event (type 'wrapup-stale-abort', data {atoms: n, ...}); set the run's pickup to an unmistakable founder-facing warning (e.g. begins '⚠️ STALE DAEMON — wrap-up did NOT run on current code. NO valid closeout/proof was produced. Restart the daemon (scripts/oz.sh restart) and re-run this priority.'); and ensure the run's FINAL status is 'failed' (RunStatus already supports 'failed'), NOT 'completed'. Do this WITHOUT routing through the existing `fail()` helper (that triggers Deb triage + throws, which is wrong here — staleness is operational, not a code fault to triage). Keep it a clean break out of the loop that then lands on a 'failed' status at run-end. The existing fresh-daemon behavior (wrapPlay present -> dispatchPlay on assigned model -> commit-gate -> pickup from Play output) and the legitimate not-stale no-Play fallback must remain BYTE-FOR-BYTE unchanged.\n2. packages/daemon/src/launcher.ts: in launchRun, after `headNow` is computed, set the new field on the RunInput before runRun, computing staleness the SAME way warnIfDaemonStale does: stale = ctx.bootSha !== 'unknown' && headNow !== 'unknown' && headNow !== ctx.bootSha. Leave warnIfDaemonStale and its 'daemon-stale' event/warn/audit exactly as-is (still want the observability). The CLI standalone path (packages/cli/src/run.ts) must NOT set daemonStale (a fresh CLI process is never stale) — leave run.ts unchanged so the field stays absent there.\n\nACCEPTANCE CRITERIA (prove with hermetic unit tests in packages/core/tests/runner.test.ts):\n(a) daemonStale=true + a wrapup directive -> NO Play dispatched, run-end status is 'failed', a 'wrapup-stale-abort' event is recorded, and pickup is the warning string (NOT directive.pickup used as a normal closeout). \n(b) daemonStale=false (or absent) + wrapPlay present -> existing Play-dispatch path is unchanged (Play runs on assigned adapter, commit-gate against Play scope, pickup from Play output).\n(c) daemonStale=false + no wrapPlay -> existing legitimate old-path fallback unchanged (pickup = directive.pickup, status completed).\n\nMUST NOT BREAK: full `pnpm -r test` green, `pnpm typecheck` clean, `pnpm check:topology` clean (runner stays core-only; daemon may import core). Do NOT touch docs, ADRs, priorities, or assignments.json in this atom. Do NOT run git/commit — the CoCoder runner commits your in-scope diff after I verify. WRITE-SCOPE: packages/core/** and packages/daemon/** only."},"at":1780158568123},{"id":"ev_043d8bcbfa854786","runId":"run_21","type":"builder-dispatch","data":{"ref":"surface:3","atom":0},"at":1780158568205},{"id":"ev_c9837b934a104ceb","runId":"run_21","type":"monitor-assessment","data":{"atom":0,"state":"done","note":"completion marker printed"},"at":1780158673643},{"id":"ev_8fff6dee06cf4d18","runId":"run_21","type":"builder-done","data":{"atom":0,"samples":8},"at":1780158673643},{"id":"ev_0a45d33ba54d4550","runId":"run_21","type":"verify-dispatch","data":{"ref":"surface:2","atom":0},"at":1780158673727},{"id":"ev_2b7b4f93b13f42b5","runId":"run_21","type":"verify-pass","data":{"atom":0,"reason":"Read the diff: daemonStale threaded from launcher (bootSha!=headNow), wrap-up now fails loud when stale (no Play, no silent fallback, status 'failed', 'wrapup-stale-abort' event) while launch stays non-blocking; fresh-daemon/no-Play paths unchanged. Ran it myself: pnpm -r test green (core 134 incl. new stale-abort test), typecheck clean, topology clean; in-scope to packages/core+daemon."},"at":1780158719611},{"id":"ev_a2b50913ea00467c","runId":"run_21","type":"commit","data":{"sha":"9b04a09291df92106e1fc2369be0d44ff5571673","files":["packages/core/src/runner/runner.ts","packages/core/tests/runner.test.ts","packages/daemon/src/launcher.ts"]},"at":1780158719707},{"id":"ev_f8466f8b65664d39","runId":"run_21","type":"wrapup","data":{"atoms":1,"forced":false},"at":1780158783895},{"id":"ev_6fa05f45d4ba4a8f","runId":"run_21","type":"run-end","data":{"status":"completed","atoms":1,"committedShas":["9b04a09291df92106e1fc2369be0d44ff5571673"],"outOfScope":[],"selfCommitted":false},"at":1780158783896}],"files":{"oscarOut":null,"oscarErr":null,"bobOut":null,"bobErr":null,"pickup":"PLAYS MECHANISM (ADR-0005/0010) — run_21 wrap-up. CONCLUSION FIRST: the mechanism is BUILT + WIRED + UNIT-PROVEN, AND this run fixed the stale-daemon trap that silently burned runs 19/20. The ONE remaining piece is the live dogfood (the Objective's 'on a real run' proof) + folding in the implementation-note docs — BOTH require a daemon RESTART first and so must run in a FRESH run (run_22). NO build work remains besides the docs note (which the dogfood's wrap-up Play should author as its real in-scope work).\n\nDONE + COMMITTED this run (branch rebuild/phase-2-oz, HEAD now 9b04a09):\n- 9b04a09 (atom 0): stale-daemon FAIL-LOUD guard. RunInput gained optional `daemonStale?:boolean`. packages/daemon/src/launcher.ts launchRun now sets it from the SAME predicate as warnIfDaemonStale (ctx.bootSha!=='unknown' && headNow!=='unknown' && headNow!==ctx.bootSha) on the RunInput before runRun; the CLI standalone path (packages/cli/src/run.ts) is unchanged so a fresh CLI process is never stale. packages/core/src/runner/runner.ts wrapup branch: a NEW first arm `if (input.daemonStale===true)` aborts loudly — NO Play dispatch, NO silent old-inline fallback; sets pickup to a '⚠️ STALE DAEMON ...restart (scripts/oz.sh restart) and re-run' warning, records a distinct `wrapup-stale-abort` event, and forces run-end status to 'failed' (via a new terminalStatus local; does NOT route through fail()/Deb-triage, since staleness is operational, not a code fault). 'Launch never blocks' is preserved — only the wrap-up refuses to fake a closeout. Fresh-daemon Play-dispatch path and the legitimate not-stale no-Play fallback are byte-for-byte unchanged.\n\nVERIFIED THIS RUN (Oscar's own evidence, not the builder's word, at 9b04a09 pre-commit): full `pnpm -r test` green (core 134 incl. a new targeted test proving stale+wrapup -> status 'failed', no cursor-agent dispatched, pickup is the warning NOT Oscar's seed, 'wrapup-stale-abort' present and no 'wrapup' event; daemon 35; adapters 10; cli 2; session-hosts 12), `pnpm typecheck` clean, `pnpm check:topology` clean (runner stays core-only). Diff was in-scope to packages/core/** + packages/daemon/** only.\n\nWHY STILL NO LIVE PROOF (verified, not assumed): the daemon serving run_21 booted at 08:22 on bootSha bf19eb7 (confirmed live via GET http://localhost:7878/health -> sha bf19eb7), which PREDATES the wrap-up-Play wiring (4bc5d57/a9af2da) AND today's guard (9b04a09). A long-lived node daemon keeps its boot-time code, so run_21's own wrap-up ran the OLD inline path (this very pickup persisted as pickup.md). Same stale-daemon trap as runs 19 and 20. The restart CANNOT be done from inside a run (it kills the run's panes; `oz stop` refuses mid-run; and the hard guardrail forbids Oscar touching the Oz daemon). So the proof MUST move to run_22, after a founder restart.\n\nSTART HERE (run_22 — do IN ORDER):\n1. RESTART THE DAEMON FIRST: `scripts/oz.sh restart` (run_21 must be torn down so no run is in flight, else stop refuses). This loads BOTH the wrap-up-Play wiring AND today's stale guard. SAFETY NET as of this run: if you forget to restart, run_22's wrap-up will now FAIL LOUD — status 'failed' + a 'wrapup-stale-abort' event + the ⚠️ pickup — instead of silently faking a 'completed' closeout. So a stale run is now unmistakable rather than a silent waste.\n2. Launch run_22 on THIS priority (plays-mechanism). It can go STRAIGHT to wrap-up (the mechanism is complete) or do one tiny real atom first; the point is the wrap-up directive.\n3. CONFIRM the Objective by watching the cursor-agent pane (group = runId): (a) the runner dispatches the wrap-up Play on cursor-agent (the assigned cheap model), (b) the Play authors the closeout, (c) the runner commits the Play's edits WITHIN the Play's writeScope (cocoder/priorities/**, docs/**, ARCHITECTURE.md, cocoder/rebuild/decisions/**), (d) pickup.md is the Play's OWN output, not the seed, and the run ends 'completed' (NOT 'failed'/stale-abort). That sequence IS the Objective's proof.\n4. DOCS — fold INTO the dogfood: have the wrap-up Play author the ADR-0005/0010 implementation note (living-ADR per ADR-0014) + any ARCHITECTURE mention as its real in-scope closeout work, which closes the docs item AND demonstrates the Play doing genuine in-scope doc work in one stroke. Only if the dogfood is a pure wrap with no doc content should you land the note as a separate Oscar governance commit afterward.\n\nOPEN FOUNDER DECISION (raised, not built — do NOT silently expand): a stale daemon also runs stale verify-gate/atom code, not just wrap-up. This run's guard only hardens the WRAP-UP (per the founder's scoped choice). Whether to extend the staleness guard to refuse/flag stale runs more broadly (e.g. block atom delegation, or a louder pre-launch signal) is a SEPARATE decision — surface it to the founder; it is arguably its own priority, not inside 'Plays mechanism'.\n\nGOTCHAS: (i) daemonStale is OPTIONAL with graceful default — absent => treated as fresh; only the daemon launcher sets it, so the CLI path and any direct runRun caller are unaffected. (ii) resolvePlayAssignment returns oscar's per-Play override (cursor-agent) when present, else inherits oscar's own cli+model — it does NOT throw for a known persona. assignments.json already pins oscar->wrap-up->cursor-agent (run_18 commit 3ae8f61). (iii) Bob's write-scope this run was packages/core/** + packages/daemon/** only — that's why no docs/ADR/assignments were touched; the wrap-up Play's own scope is what enables the docs note. (iv) HEAD is 9b04a09, tree clean.","record":"# Run run_21\n\n- **Workspace:** CoCoder (dogfood) (`cocoder`) — /Volumes/NAS LOCAL/CoCoder\n- **Priority:** Plays mechanism (ADR-0005) — proven by extracting wrap-up as the first Play (`plays-mechanism`)\n- **Status:** completed\n- **Started:** 2026-05-30T15:00:27.239Z  ·  **Ended:** 2026-05-30T16:33:03.896Z\n\n## Sessions\n\n- **oscar** — `surface:2` — exit n/a\n- **bob** — `surface:3` — exit n/a\n- **deb** — `surface:4` — exit n/a\n\n## Work items\n\n- **oscar → bob** [done] — scope: packages/**\n  - Task: Make the wrap-up path FAIL LOUD on a stale daemon instead of silently faking a closeout. CONTEXT: a long-lived Oz daemon serves the orchestration code it booted with; when repo HEAD advances past the daemon's bootSha, the daemon is 'stale' and runs old code. Today the daemon DETECTS this (packages/daemon/src/launcher.ts warnIfDaemonStale -> records a 'daemon-stale' event, console.warns, audits) but is observability-only and 'never blocks a launch'. The failure we are fixing: on a stale daemon the boot-time launcher does NOT pass wrapPlay/wrapPlayAssignment into RunInput, so the runner's wrapup branch (packages/core/src/runner/runner.ts ~line 306) silently takes the OLD inline fallback (pickup = directive.pickup) and the run ends status 'completed' as if it were a real closeout. This has silently burned three runs (19/20/21). NOTE the fix takes effect on the NEXT daemon restart, not the current process — that is expected; do not try to affect the running daemon.\n\nDESIGN (founder-approved): keep 'launch never blocks' — the daemon must still launch the run. Only the WRAP-UP must refuse to silently degrade when the daemon is stale.\n\nCHANGES:\n1. packages/core/src/runner/runner.ts + its RunInput type: add a new OPTIONAL field `daemonStale?: boolean` to RunInput (default undefined/false). In the `if (directive.kind === 'wrapup')` branch, BEFORE the existing `if (input.wrapPlay && input.wrapPlayAssignment)` check, add: if `input.daemonStale` is true, FAIL LOUD — do NOT dispatch the Play, do NOT take the silent old-path fallback, do NOT mark the run 'completed' with a normal-looking pickup. Instead: record a distinct event (type 'wrapup-stale-abort', data {atoms: n, ...}); set the run's pickup to an unmistakable founder-facing warning (e.g. begins '⚠️ STALE DAEMON — wrap-up did NOT run on current code. NO valid closeout/proof was produced. Restart the daemon (scripts/oz.sh restart) and re-run this priority.'); and ensure the run's FINAL status is 'failed' (RunStatus already supports 'failed'), NOT 'completed'. Do this WITHOUT routing through the existing `fail()` helper (that triggers Deb triage + throws, which is wrong here — staleness is operational, not a code fault to triage). Keep it a clean break out of the loop that then lands on a 'failed' status at run-end. The existing fresh-daemon behavior (wrapPlay present -> dispatchPlay on assigned model -> commit-gate -> pickup from Play output) and the legitimate not-stale no-Play fallback must remain BYTE-FOR-BYTE unchanged.\n2. packages/daemon/src/launcher.ts: in launchRun, after `headNow` is computed, set the new field on the RunInput before runRun, computing staleness the SAME way warnIfDaemonStale does: stale = ctx.bootSha !== 'unknown' && headNow !== 'unknown' && headNow !== ctx.bootSha. Leave warnIfDaemonStale and its 'daemon-stale' event/warn/audit exactly as-is (still want the observability). The CLI standalone path (packages/cli/src/run.ts) must NOT set daemonStale (a fresh CLI process is never stale) — leave run.ts unchanged so the field stays absent there.\n\nACCEPTANCE CRITERIA (prove with hermetic unit tests in packages/core/tests/runner.test.ts):\n(a) daemonStale=true + a wrapup directive -> NO Play dispatched, run-end status is 'failed', a 'wrapup-stale-abort' event is recorded, and pickup is the warning string (NOT directive.pickup used as a normal closeout). \n(b) daemonStale=false (or absent) + wrapPlay present -> existing Play-dispatch path is unchanged (Play runs on assigned adapter, commit-gate against Play scope, pickup from Play output).\n(c) daemonStale=false + no wrapPlay -> existing legitimate old-path fallback unchanged (pickup = directive.pickup, status completed).\n\nMUST NOT BREAK: full `pnpm -r test` green, `pnpm typecheck` clean, `pnpm check:topology` clean (runner stays core-only; daemon may import core). Do NOT touch docs, ADRs, priorities, or assignments.json in this atom. Do NOT run git/commit — the CoCoder runner commits your in-scope diff after I verify. WRITE-SCOPE: packages/core/** and packages/daemon/** only.\n\n## Commits\n\n- `9b04a09291df92106e1fc2369be0d44ff5571673` — plays-mechanism: atom 0 via CoCoder run run_21\n  - packages/core/src/runner/runner.ts\n  - packages/core/tests/runner.test.ts\n  - packages/daemon/src/launcher.ts\n\n## Event log\n\n- 2026-05-30T15:00:27.240Z `daemon-stale`\n- 2026-05-30T15:00:27.240Z `run-start`\n- 2026-05-30T15:00:27.450Z `preflight`\n- 2026-05-30T15:00:27.564Z `preflight`\n- 2026-05-30T15:00:32.727Z `spawn`\n- 2026-05-30T15:00:32.937Z `spawn`\n- 2026-05-30T15:00:33.057Z `preflight`\n- 2026-05-30T15:00:33.317Z `spawn`\n- 2026-05-30T16:29:28.123Z `delegation`\n- 2026-05-30T16:29:28.205Z `builder-dispatch`\n- 2026-05-30T16:31:13.643Z `monitor-assessment`\n- 2026-05-30T16:31:13.643Z `builder-done`\n- 2026-05-30T16:31:13.727Z `verify-dispatch`\n- 2026-05-30T16:31:59.611Z `verify-pass`\n- 2026-05-30T16:31:59.707Z `commit`\n- 2026-05-30T16:33:03.895Z `wrapup`\n- 2026-05-30T16:33:03.896Z `run-end`\n"},"diffs":[{"sha":"9b04a09291df92106e1fc2369be0d44ff5571673","diff":"commit 9b04a09291df92106e1fc2369be0d44ff5571673\nAuthor: Anthony Franco <anthony@francoinc.com>\nDate:   Sat May 30 10:31:59 2026 -0600\n\n    plays-mechanism: atom 0 via CoCoder run run_21\n\ndiff --git a/packages/core/src/runner/runner.ts b/packages/core/src/runner/runner.ts\nindex b3c6cfd..915b868 100644\n--- a/packages/core/src/runner/runner.ts\n+++ b/packages/core/src/runner/runner.ts\n@@ -81,6 +81,8 @@ export interface RunInput {\n   /** Resolved wrap-up Play + per-(persona, Play) assignment; when present, the runner dispatches the Play to author closeout. */\n   readonly wrapPlay?: Play\n   readonly wrapPlayAssignment?: PlayAssignment\n+  /** Daemon launch-time guard: true when the long-lived daemon is serving code older than repo HEAD. */\n+  readonly daemonStale?: boolean\n }\n \n export interface RunResult {\n@@ -289,6 +291,7 @@ export async function runRun(deps: RunnerDeps, input: RunInput): Promise<RunResu\n   const outOfScope: string[] = []\n   let selfCommitted = false\n   let pickup: string | null = null\n+  let terminalStatus: RunStatus | null = null\n   let n = 0\n   let consecutiveRejects = 0\n \n@@ -304,7 +307,14 @@ export async function runRun(deps: RunnerDeps, input: RunInput): Promise<RunResu\n     }\n \n     if (directive.kind === 'wrapup') {\n-      if (input.wrapPlay && input.wrapPlayAssignment) {\n+      if (input.daemonStale === true) {\n+        pickup =\n+          '⚠️ STALE DAEMON - wrap-up did NOT run on current code. NO valid closeout/proof was produced. ' +\n+          'Restart the daemon (scripts/oz.sh restart) and re-run this priority.'\n+        terminalStatus = 'failed'\n+        store.recordEvent({ runId: run.id, type: 'wrapup-stale-abort', data: { atoms: n } })\n+        log(`wrap-up aborted after ${n} atom(s): stale daemon`)\n+      } else if (input.wrapPlay && input.wrapPlayAssignment) {\n         const headBeforeWrap = await git.headSha(workspace.path)\n         const task =\n           `Run ${run.id} on priority ${priority.id}. ${n} atom(s) were delegated; commits so far: ${committedShas.join(', ') || 'none'}.\\n\\n` +\n@@ -461,7 +471,7 @@ export async function runRun(deps: RunnerDeps, input: RunInput): Promise<RunResu\n \n   // ── Wrap-up: pickup brief (continuation; F8) + run record ───────────────────────────────────────\n   const pickupPath = pickup ? await io.writePickup(runDir, pickup) : null\n-  const status: RunStatus = outOfScope.length > 0 ? 'pending-scope-decision' : 'completed'\n+  const status: RunStatus = terminalStatus ?? (outOfScope.length > 0 ? 'pending-scope-decision' : 'completed')\n   store.setRunStatus(run.id, status)\n   store.recordEvent({\n     runId: run.id,\ndiff --git a/packages/core/tests/runner.test.ts b/packages/core/tests/runner.test.ts\nindex fd5f97f..d0a4947 100644\n--- a/packages/core/tests/runner.test.ts\n+++ b/packages/core/tests/runner.test.ts\n@@ -231,7 +231,7 @@ describe('runRun (multi-atom loop)', () => {\n           },\n         }),\n       }),\n-      { ...input, runsRoot, wrapPlay, wrapPlayAssignment },\n+      { ...input, runsRoot, wrapPlay, wrapPlayAssignment, daemonStale: false },\n     )\n \n     expect(adapterCalls).toContain('cursor-agent')\n@@ -253,6 +253,38 @@ describe('runRun (multi-atom loop)', () => {\n     expect((wrap?.data as { play?: string }).play).toBe('wrap-up')\n   })\n \n+  test('stale daemon aborts wrap-up loudly without dispatching a Play or using Oscar pickup', async () => {\n+    const store = openRunStore(':memory:')\n+    const pickupWrites: string[] = []\n+    const adapterCalls: string[] = []\n+\n+    const result = await runRun(\n+      baseDeps({\n+        store,\n+        io: fakeIO({ directives: [wrapup('normal-looking closeout')], pickupWrites }),\n+        getAdapter: (cli) => {\n+          adapterCalls.push(cli)\n+          return okAdapter\n+        },\n+      }),\n+      { ...input, wrapPlay, wrapPlayAssignment, daemonStale: true },\n+    )\n+\n+    expect(result.status).toBe('failed')\n+    expect(result.atoms).toBe(0)\n+    expect(adapterCalls).not.toContain('cursor-agent')\n+    expect(pickupWrites).toHaveLength(1)\n+    expect(pickupWrites[0]).toMatch(/^⚠️ STALE DAEMON/)\n+    expect(pickupWrites[0]).not.toContain('normal-looking closeout')\n+    const events = store.listEvents(result.runId)\n+    expect(events.some((e) => e.type === 'wrapup')).toBe(false)\n+    const abort = events.find((e) => e.type === 'wrapup-stale-abort')\n+    expect(abort?.data).toEqual({ atoms: 0 })\n+    const end = events.find((e) => e.type === 'run-end')\n+    expect((end?.data as { status?: string }).status).toBe('failed')\n+    expect(store.getRun(result.runId)?.status).toBe('failed')\n+  })\n+\n   test('falls back to Oscar pickup without dispatching a Play when no wrap Play is configured', async () => {\n     const store = openRunStore(':memory:')\n     const pickupWrites: string[] = []\ndiff --git a/packages/daemon/src/launcher.ts b/packages/daemon/src/launcher.ts\nindex 708a2e6..7daff88 100644\n--- a/packages/daemon/src/launcher.ts\n+++ b/packages/daemon/src/launcher.ts\n@@ -125,6 +125,10 @@ export async function launchRun(ctx: OzContext, workspaceId: string, priorityId:\n     return { status: 400, body: { error: err instanceof Error ? err.message : String(err) } }\n   }\n   const headNow = await headShaOrUnknown(ctx, input.workspace.path)\n+  input = {\n+    ...input,\n+    daemonStale: ctx.bootSha !== 'unknown' && headNow !== 'unknown' && headNow !== ctx.bootSha,\n+  }\n \n   let runId: string | null = null\n   const deps: RunnerDeps = {\n"}]}