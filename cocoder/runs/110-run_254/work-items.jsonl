{"id":"wi_6cd4d7d40c864920","runId":"run_254","sourcePersona":"oscar","targetPersona":"bob","task":"Fix the symmetric 409-masking defect in the ticket-launch path in packages/ui/src/renderer/App.tsx. CONTEXT: run_253 already fixed `doLaunch` (App.tsx ~574-608) so that a 409 is only shown as the generic 'A run is already in flight for this workspace.' when the daemon error actually matches in-flight (`res.status === 409 && /in flight/i.test(res.error)`), and ANY OTHER 409 falls through to the `else` branch that surfaces `res.error` verbatim. But `doLaunchTicket` (App.tsx ~613-639) was missed: its 409 handler is `else if (res.status === 409)` and hardcodes 'A run is already in flight for this workspace.' for EVERY 409 — masking real, distinct 409 reasons. The most important masked case is the pending-close refusal in packages/daemon/src/launcher.ts:794 ('ticket \"X\" already has run Y awaiting founder close confirmation ...'), which a founder would currently see as the wrong 'already in flight' message.\n\nWHAT TO CHANGE: In `doLaunchTicket`, branch the 409 handler exactly the way `doLaunch` already does — guard the generic 'already in flight' message on `res.status === 409 && /in flight/i.test(res.error)`, and for any other 409 fall through to surfacing the daemon's real `res.error` verbatim (same shape as doLaunch's final `else`: `const message = res.error || `Launch failed (${res.status}).``, setLaunchProgressError + notify('err', ...)). Match the surrounding style and the established doLaunch pattern precisely; do not invent a new pattern.\n\nMUST NOT BREAK: do not touch `doLaunch`, `launchIndependentHandoff`, or any daemon/launcher code — this atom is the ticket-launch UI handler only. Do not change the in-flight behavior (a genuine in-flight 409 must still show the generic message). Preserve all unrelated code.\n\nTESTS: add or extend a test in packages/ui/tests/live-app.test.tsx that asserts a NON-in-flight 409 from the ticket-launch path (e.g. the pending-close refusal error string) is surfaced verbatim to the founder, NOT replaced by 'already in flight'; and keep/confirm a test that a genuine in-flight 409 still shows the generic message.\n\nACCEPTANCE: (1) launching a fix run for a ticket that returns a non-in-flight 409 shows the daemon's real error verbatim; (2) a genuine in-flight 409 still shows 'A run is already in flight for this workspace.'; (3) `pnpm --filter @cocoder/ui test` (or the repo's UI test command) passes; (4) typecheck passes. Report the exact diff and the test command output.","writeScope":["packages/**","templates/**","docs/**","ARCHITECTURE.md","package.json","pnpm-lock.yaml","pnpm-workspace.yaml","tsconfig*.json","eslint.config.*","vitest.config.*","scripts/**"],"status":"done","createdAt":1782514672357}
