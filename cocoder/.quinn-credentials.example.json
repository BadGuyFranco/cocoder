{
  "_comment": "Quinn credentials template. Copy to cocoder/local/.quinn-credentials.json and fill in real values. The cocoder/local/ directory is gitignored except for its inner .gitignore and README.md, so the populated file will NOT be tracked. Pass --credentials <path> to override the default location.",
  "staging": {
    "example.user@your-domain.test": {
      "password": "<plain-text-password-here>"
    }
  },
  "production": {
    "example.user@your-domain.test": {
      "password": "<plain-text-password-here>"
    }
  }
}
