{
  "_comment": "Quinn credentials template. Copy to local/secrets/.quinn-credentials.json (the install-private zone) and fill in real values; that zone is never git-tracked. Pass --credentials <path> to override the default location.",
  "staging": {
    "example.user@your-domain.test": {
      "password": "<plain-text-password-here>"
    }
  },
  "production": {
    "example.user@your-domain.test": {
      "password": "<plain-text-password-here>"
    }
  }
}
