# Workspace Segmentation Owner Map

Atom 0, run_135 (analysis). Run_136 partially implemented concerns **2** (chat target picker) and
**13** (cmux group labels) plus Objective **1** visual split (UI only — no storage changes). Storage,
migration, counters, and concurrency rows below remain design targets until their atoms land.

Evidence searched: `rg` over `packages/daemon`, `packages/core`, `packages/ui`, `packages/session-hosts`; `sqlite3 local/cocoder.db '.schema'`; `PRAGMA table_info(...)` for `workspace`, `run`, `session`, `work_item`, `commit_link`, `event`, and `run_counter`.

## Concern Owners

| Concern | Source-of-truth owner | Consumers |
|---|---|---|
| 1. Workspace identity | `packages/daemon/src/registry.ts:21` `RegistryWorkspace`; `readWorkspaces()` at `packages/daemon/src/registry.ts:167`; `findWorkspace()` at `packages/daemon/src/registry.ts:173`; install-local files from `workspaceFilePath()` / `workspaceDirectory()` at `packages/daemon/src/registry.ts:53` and `packages/daemon/src/registry.ts:57`. The DB `workspace` table is derived FK state, not the owner (`packages/core/src/store/schema.ts:9`). | Daemon `listWorkspaces()` (`packages/daemon/src/routes.ts:346`); all workspace-scoped read routes (`packages/daemon/src/routes.ts:351`, `packages/daemon/src/routes.ts:358`, `packages/daemon/src/routes.ts:365`, `packages/daemon/src/routes.ts:391`); run detail diff lookup (`packages/daemon/src/routes.ts:426`); workspace create/update/delete (`packages/daemon/src/routes.ts:642`, `packages/daemon/src/routes.ts:678`, `packages/daemon/src/routes.ts:718`); launcher `buildRunInput()` / `launchRun()` / support and repair paths (`packages/daemon/src/launcher.ts:149`, `packages/daemon/src/launcher.ts:485`, `packages/daemon/src/launcher.ts:848`, `packages/daemon/src/launcher.ts:1026`); UI `loadWorkspaces()` (`packages/ui/app/live.ts:41`) and `App` active workspace state (`packages/ui/app/App.tsx:67`). |
| 2. Workspace selection in chat | Renderer owner: `App` `chatTarget` state at `packages/ui/app/App.tsx:71` (independent of workspace-tab `activeId`); `ChatTargetPicker` in `packages/ui/app/sections/dashboard/OzChat.tsx` exposes loaded workspaces plus **Global Oz** (`null` target); `onSend()` resolves `chatTarget ?? GLOBAL_CHAT_KEY` and passes the selected workspace id (or omits for global) through `sendOzMessage()` at `packages/ui/app/App.tsx:269`; Electron main sends `{ text, workspaceId }` at `packages/ui/electron/daemon-client.ts:101`. Daemon parser accepts an omitted `workspaceId` (`packages/daemon/src/oz-chat.ts:229`); mutating commands call `missingWorkspace()` when no target (`packages/daemon/src/oz-chat.ts:126`, `packages/daemon/src/oz-chat.ts:247`). | `WorkspaceTabs` in `ShellControls.tsx` drive tab selection separately from chat target; dashboard `OzChatPanel` receives `chatTarget` / `chatTargets` / `onChatTargetChange` (`packages/ui/app/sections/dashboard/Dashboard.tsx`); daemon chat command execution (`packages/daemon/src/oz-chat.ts:117`). **Run_136:** first-class Global Oz UI state shipped; chat target can differ from the active workspace tab. |
| 3. Priority reads and ticket reads | Priorities: tracked files under `<workspace>/cocoder/priorities/*.md`; parser `loadPriority()` at `packages/core/src/priorities/loader.ts:29`; daemon summary reader `readPriorities()` at `packages/daemon/src/priority-order.ts:42`. Tickets: tracked files under `<workspace>/cocoder/tickets/{open,closed}/*.md`; parser `loadTicket()` at `packages/core/src/tickets/loader.ts:53`; reader `readTickets()` at `packages/core/src/tickets/loader.ts:97`. | Daemon `listPriorities()` (`packages/daemon/src/routes.ts:351`) and `listTickets()` (`packages/daemon/src/routes.ts:358`); launcher `buildRunInput()` loads priority files (`packages/daemon/src/launcher.ts:149`); ticket launch reads tickets (`packages/daemon/src/launcher.ts:490`); UI loads both via `loadWsData()` (`packages/ui/app/live.ts:57`); create flows round-trip through these readers (`packages/daemon/src/routes.ts:579`, `packages/daemon/src/routes.ts:620`). |
| 4. Ticket numbering | `nextTicketId(ticketsDir)` scans `open` and `closed` ticket filenames and returns max + 1 at `packages/core/src/tickets/loader.ts:103`; daemon `createTicket()` owns the call at `packages/daemon/src/routes.ts:602`. | Ticket create endpoint (`packages/daemon/src/routes.ts:831`); Electron bridge `createTicketViaDaemon()` (`packages/ui/electron/tickets-create.ts:9`); renderer `handleCreateTicket()` (`packages/ui/app/App.tsx:432`). No DB counter exists for tickets. |
| 5. Priority ordering | Tracked `<workspace>/cocoder/priorities/order.json`; daemon helper `priorityOrderPath()` at `packages/daemon/src/priority-order.ts:22`; `readManifest()` at `packages/daemon/src/priority-order.ts:33`; `writePriorityOrder()` at `packages/daemon/src/priority-order.ts:84`. | `readPriorities()` applies the manifest (`packages/daemon/src/priority-order.ts:62`); daemon reorder route (`packages/daemon/src/routes.ts:550`, `packages/daemon/src/routes.ts:807`); Electron fallback cache `setPriorityOrder(workspaceId, order)` at `packages/ui/electron/store.ts:48`; renderer `loadOrder()` / `persistOrder()` (`packages/ui/app/live.ts:187`, `packages/ui/app/live.ts:190`) and drag handler (`packages/ui/app/App.tsx:281`). |
| 6. Run/session durable history | `packages/core/src/store/schema.ts:8` `SCHEMA_SQL`; domain contract `RunStore` at `packages/core/src/store/types.ts:87`; SQLite implementation `SqliteRunStore` at `packages/core/src/store/sqlite-store.ts:123`. Tables: `workspace`, `run`, `session`, `work_item`, `commit_link`, `event`, `run_counter`. | Runner writes run/session/work/event/commit rows (`packages/core/src/runner/runner.ts:297`, `packages/core/src/runner/runner.ts:428`, `packages/core/src/runner/agent-step.ts:114`, `packages/core/src/commit-gate/gate.ts:82`); daemon playbook path writes run rows (`packages/daemon/src/launcher.ts:595`); run list/detail reads (`packages/daemon/src/routes.ts:409`, `packages/daemon/src/routes.ts:415`); Deb recurrence reads fault history (`packages/core/src/runner/runner.ts:574`); run record is generated from DB rows (`packages/core/src/runner/record.ts:14`). |
| 7. Run/session live state | Process-local `OzContext`: `liveRefs`, `inFlight`, `stopControllers`, and `events` at `packages/daemon/src/context.ts:83`, `packages/daemon/src/context.ts:87`, `packages/daemon/src/context.ts:90`, `packages/daemon/src/context.ts:92`; constructed in `createOzServer()` at `packages/daemon/src/server.ts:129`. cmux driver local maps `#sessions` and `#groups` at `packages/session-hosts/src/cmux/driver.ts:48`. | `trackingHost()` mirrors spawned refs into `liveRefs` (`packages/daemon/src/launcher.ts:81`); launch reserve/release uses `inFlight` (`packages/daemon/src/launcher.ts:469`, `packages/daemon/src/launcher.ts:432`); stop/nudge/support-commit use `stopControllers`/`inFlight` (`packages/daemon/src/launcher.ts:795`, `packages/daemon/src/launcher.ts:818`, `packages/daemon/src/launcher.ts:843`); run detail uses `liveRefs` for deep links (`packages/daemon/src/routes.ts:420`); cmux status/show/send use `#sessions` (`packages/session-hosts/src/cmux/driver.ts:110`, `packages/session-hosts/src/cmux/driver.ts:142`). |
| 8. Run IDs and display numbers | DB `run_counter` table at `packages/core/src/store/schema.ts:86`; allocation in `SqliteRunStore.createRun()` with atomic `UPDATE ... RETURNING` at `packages/core/src/store/sqlite-store.ts:141`. Current counter is global, not per workspace. | Runner creates runs (`packages/core/src/runner/runner.ts:298`); daemon playbook path creates runs (`packages/daemon/src/launcher.ts:596`); cmux run workspace display label derives numeric suffix from `run.id` at `packages/core/src/runner/runner.ts:365`; UI displays run ids from daemon summaries/details (`packages/ui/app/live.ts:69`, `packages/ui/app/App.tsx:314`). |
| 9. Run directories | Daemon context `runsRoot: join(opts.cocoderHome, 'local', 'runs')` at `packages/daemon/src/server.ts:118`; runner uses `join(runsRoot, run.id)` at `packages/core/src/runner/runner.ts:300`; run-dir reader `readRunDir()` at `packages/daemon/src/rundir.ts:30`. | Runner IO creates and writes run-dir files (`packages/core/src/runner/io.ts:105`); launcher reads pickup from prior run (`packages/daemon/src/launcher.ts:121`) and writes nudge files (`packages/daemon/src/launcher.ts:823`); run detail reads captured artifacts (`packages/daemon/src/routes.ts:424`). |
| 10. Status feeds | Durable run status: `run.status` in `RunStore` (`packages/core/src/store/types.ts:21`, `packages/core/src/store/sqlite-store.ts:166`). Live Deb feed: pure projection `renderDebStatus()` at `packages/core/src/runner/status.ts:67`; filesystem writer `writeDebStatus()` at `packages/core/src/runner/io.ts:127`. Dashboard feed: daemon `/runs` and `/runs/:id` routes (`packages/daemon/src/routes.ts:409`, `packages/daemon/src/routes.ts:415`). | Runner refreshes Deb status at transitions (`packages/core/src/runner/runner.ts:528`); UI `loadWsData()` reads `/runs?workspace=` (`packages/ui/app/live.ts:61`); selected/detail polling reads `/runs/:id` (`packages/ui/app/live.ts:81`, `packages/ui/app/App.tsx:174`); dashboard launch-block state reads running statuses (`packages/ui/app/sections/dashboard/Dashboard.tsx:212`). |
| 11. Event hints | Daemon-local `OzEvent` / `OzEventBus` in `packages/daemon/src/context.ts:28` and `createOzEventBus()` at `packages/daemon/src/context.ts:43`; SSE endpoint `streamOzEvents()` at `packages/daemon/src/routes.ts:453`; lifecycle emitter helper `emitOzEvent()` at `packages/daemon/src/launcher.ts:77`. | Launcher emits `run-created`, `run-settled`, stop/nudge/teardown/support events (`packages/daemon/src/launcher.ts:572`, `packages/daemon/src/launcher.ts:451`, `packages/daemon/src/launcher.ts:778`, `packages/daemon/src/launcher.ts:804`, `packages/daemon/src/launcher.ts:833`, `packages/daemon/src/launcher.ts:881`); Electron SSE bridge `startOzEventStream()` (`packages/ui/electron/events-stream.ts:97`); renderer event handler refreshes workspace/run state (`packages/ui/app/App.tsx:191`). |
| 12. Git locks / dirty-tree guards | There is no separate git lock file owner. The runtime lock is `OzContext.inFlight` keyed by workspace (`packages/daemon/src/context.ts:87`) plus launch reservation in `launchRun()` (`packages/daemon/src/launcher.ts:469`). Dirty-tree owner is `runRun()` direct-mode preflight (`packages/core/src/runner/runner.ts:316`, `packages/core/src/runner/runner.ts:335`) using `Git.changedFiles()` (`packages/core/src/commit-gate/git.ts:136`) and `partitionByScope()`. Commit boundary owner is `runCommitGate()` at `packages/core/src/commit-gate/gate.ts:57`. SQLite writer locking is implicit in `openRunStore()` / `node:sqlite` module comment (`packages/core/src/store/sqlite-store.ts:1`). | UI launch buttons reflect a single active run per workspace (`packages/ui/app/sections/dashboard/Dashboard.tsx:10`, `packages/ui/app/sections/dashboard/Dashboard.tsx:212`); daemon launch, restart, repair, authoring, support commit all inspect `inFlight` (`packages/daemon/src/launcher.ts:469`, `packages/daemon/src/launcher.ts:918`, `packages/daemon/src/launcher.ts:1017`, `packages/daemon/src/launcher.ts:1062`, `packages/daemon/src/launcher.ts:843`); runner quarantine uses `Git.restoreToHead()` (`packages/core/src/runner/runner.ts:773`, `packages/core/src/commit-gate/git.ts:157`). |
| 13. cmux/session labels | Run-level cmux group label owner: `groupLabel()` at `packages/core/src/runner/labels.ts:29` — format `workspace · target-type:slug #run-number`; persona pane label owner: `paneLabel()` at `packages/core/src/runner/labels.ts:36` (persona/LLM/model only). Runner and playbook spawn paths both call `groupLabel()` (`packages/core/src/runner/runner.ts`, `packages/daemon/src/launcher.ts`). cmux applies workspace/pane labels in `CmuxSessionHost.spawn()` at `packages/session-hosts/src/cmux/driver.ts:60`, `packages/session-hosts/src/cmux/driver.ts:76`, and `packages/session-hosts/src/cmux/driver.ts:85`. | Runner passes `group`, `groupLabel`, and `label` for Oscar/Bob/Deb; cmux durable refs stored in `session.workspace_ref`; teardown closes by durable refs (`packages/daemon/src/launcher.ts:668`). **Run_136:** group label includes workspace name + target type/slug + run number on both priority-run and playbook spawn paths. |

## DB Classification Table

Verdicts are field-level because several rows mix portable history with machine-local runtime refs. Portable path proposals below are proposals for founder/owner-map review, not implementation decisions.

| Table.field | Current writer / reader | Classification | Portable owner proposal |
|---|---|---|---|
| `workspace.id` | `SqliteRunStore.upsertWorkspace()` (`packages/core/src/store/sqlite-store.ts:132`) | Machine-local routing mirror of registry identity; DB row is derived from install-local registry. | None. If portable identity is desired, propose `cocoder/workspace.json` with `{id,name}` only; roots stay install-local unless separately approved. |
| `workspace.path` | same | Machine-local operational state; absolute repo path. | None. |
| `workspace.name` | same | Machine-local registry mirror today. | Optional proposal with `workspace.id`: `cocoder/workspace.json`. |
| `run.id` | `SqliteRunStore.createRun()` (`packages/core/src/store/sqlite-store.ts:141`) | Workspace-portable durable history, but allocated from current global machine-local counter. | `cocoder/runs/<run-id>/run.json` with run identity and target. |
| `run.workspace_id` | same | Workspace-portable history link once segmented; currently also local routing FK. | `cocoder/runs/<run-id>/run.json` or omitted inside a workspace-local file tree. |
| `run.priority_id` | same | Workspace-portable durable target reference; compatibility sentinel for ticket/playbook runs. | `cocoder/runs/<run-id>/run.json` target object. |
| `run.playbook_id` | same | Workspace-portable durable target discriminator. | `cocoder/runs/<run-id>/run.json` target object. |
| `run.ticket_id` | same | Workspace-portable durable target discriminator. | `cocoder/runs/<run-id>/run.json` target object. |
| `run.status` | `setRunStatus()` (`packages/core/src/store/sqlite-store.ts:166`) | Workspace-portable durable run outcome. | `cocoder/runs/<run-id>/run.json`. |
| `run.created_at`, `run.ended_at` | `createRun()` / `setRunStatus()` | Workspace-portable durable history timestamps. | `cocoder/runs/<run-id>/run.json`. |
| `run.worktree_path`, `run.run_branch`, `run.integration_status` | Legacy columns present in `local/cocoder.db`; schema notes they are inert (`packages/core/src/store/schema.ts:107`). | Machine-local obsolete operational state; no current reader/writer references. | None. |
| `session.id` | `createSession()` (`packages/core/src/store/sqlite-store.ts:185`) | Workspace-portable durable session record id. | `cocoder/runs/<run-id>/sessions.jsonl`. |
| `session.run_id` | same | Workspace-portable history link. | `cocoder/runs/<run-id>/sessions.jsonl`. |
| `session.persona` | same | Workspace-portable durable persona reference. | `cocoder/runs/<run-id>/sessions.jsonl`. |
| `session.session_ref` | same | Machine-local operational state; cmux surface ref. | None, or only redacted/display copy in `cocoder/runs/<run-id>/sessions.jsonl` if founder wants audit visibility. |
| `session.workspace_ref` | same | Machine-local operational state; cmux workspace/container ref for teardown. | None. |
| `session.started_at`, `session.exit_code` | same / `setSessionExit()` | Mixed: start and exit summary are portable history; exit can also be runtime diagnostic. | `cocoder/runs/<run-id>/sessions.jsonl` without machine refs. |
| `work_item.id` | `createWorkItem()` (`packages/core/src/store/sqlite-store.ts:205`) | Workspace-portable durable atom history. | `cocoder/runs/<run-id>/work-items.jsonl`. |
| `work_item.run_id` | same | Workspace-portable history link. | `cocoder/runs/<run-id>/work-items.jsonl`. |
| `work_item.source_persona`, `work_item.target_persona` | same | Workspace-portable durable persona references. | `cocoder/runs/<run-id>/work-items.jsonl`. |
| `work_item.task` | same | Workspace-portable durable work history. | `cocoder/runs/<run-id>/work-items.jsonl`. |
| `work_item.write_scope` | same | Workspace-portable durable scope evidence. | `cocoder/runs/<run-id>/work-items.jsonl` as JSON array. |
| `work_item.status`, `work_item.created_at` | `createWorkItem()` / `setWorkItemStatus()` | Workspace-portable durable atom state. | `cocoder/runs/<run-id>/work-items.jsonl`. |
| `commit_link.id` | `recordCommitLink()` (`packages/core/src/store/sqlite-store.ts:244`) | Workspace-portable durable commit history. | `cocoder/runs/<run-id>/commits.jsonl`. |
| `commit_link.run_id`, `commit_link.work_item_id` | same | Workspace-portable history links. | `cocoder/runs/<run-id>/commits.jsonl`. |
| `commit_link.commit_sha`, `commit_link.message`, `commit_link.files`, `commit_link.created_at` | same | Workspace-portable durable commit receipt. | `cocoder/runs/<run-id>/commits.jsonl`. |
| `commit_link.kind`, `commit_link.merge_sha`, `commit_link.trunk_parent` | Legacy columns present in existing DB; schema says branch->trunk merge link was removed (`packages/core/src/store/schema.ts:67`). | Machine-local obsolete/inert integration lane residue. | None unless migrated into archived legacy import notes. |
| `event.id` | `recordEvent()` (`packages/core/src/store/sqlite-store.ts:269`) | Workspace-portable durable event identity. | `cocoder/runs/<run-id>/events.jsonl`. |
| `event.run_id` | same | Workspace-portable history link. | `cocoder/runs/<run-id>/events.jsonl`. |
| `event.type`, `event.at` | same | Workspace-portable durable event stream. | `cocoder/runs/<run-id>/events.jsonl`. |
| `event.data` | same | Mixed. Many payloads are portable (`atom`, `status`, `sha`, `files`); some carry machine-local paths or refs (`runDir`, `outPath`, `ref`, `statePath`). | `cocoder/runs/<run-id>/events.jsonl` with a documented portable schema/redaction rule before migration. |
| `run_counter.id` | schema seed at `packages/core/src/store/schema.ts:86` | Machine-local singleton row, but conceptually workspace-history counter should become portable per workspace. | Proposal: `cocoder/runs/counter.json` with `{nextRunNumber}` or derive from `cocoder/runs/run_*/` listing. |
| `run_counter.next` | `createRun()` atomic update (`packages/core/src/store/sqlite-store.ts:146`) | Currently machine-local global shared state; desired segmentation likely workspace-portable counter. | Proposal: `cocoder/runs/counter.json`; founder must decide global vs per-workspace numbering. |

## Collision / Concurrency Assumptions

These are the current shared resources and single-workspace assumptions that workspace segmentation must either preserve deliberately or split.

| Resource / assumption | Current location | Collision risk |
|---|---|---|
| One install-wide SQLite DB at `local/cocoder.db`. | `createOzServer()` opens `join(opts.cocoderHome, 'local', 'cocoder.db')` at `packages/daemon/src/server.ts:119`; `sqlite-store.ts` notes one DB file and SQLite writer lock at `packages/core/src/store/sqlite-store.ts:1`. | All workspaces share one DB connection and writer lock. Durable portable history and machine-local state are intermixed. |
| Global run id sequence, not per workspace. | `run_counter` schema at `packages/core/src/store/schema.ts:81`; `createRun()` at `packages/core/src/store/sqlite-store.ts:141`; store test explicitly expects `other` workspace to receive `run_3` after two `cocoder` runs (`packages/core/tests/store.test.ts:37`). | Per-workspace portable history cannot replay run numbers without either namespacing ids or moving the counter. |
| Global run directory root `local/runs/<runId>`, not `local/workspaces/<id>/runs`. | `runsRoot` set at `packages/daemon/src/server.ts:118`; runner `join(runsRoot, run.id)` at `packages/core/src/runner/runner.ts:300`; `readRunDir()` joins only `runsRoot, runId` at `packages/daemon/src/rundir.ts:30`. | A future per-workspace run id could collide on disk immediately. Existing `resumeFromRunId` also reads by bare id (`packages/daemon/src/launcher.ts:121`). |
| One daemon-wide SSE stream. | `/oz/events` route at `packages/daemon/src/routes.ts:453`; event bus has one subscriber set at `packages/daemon/src/context.ts:43`; Electron bridges the same stream to all windows at `packages/ui/electron/events-stream.ts:87`. | Events carry optional `workspaceId`, but the stream itself is not partitioned. A missing workspace hint falls back to the active tab in the renderer. |
| Renderer falls back to active workspace when an event hint lacks `workspaceId`. | `const wsId = event.workspaceId ?? activeIdRef.current` at `packages/ui/app/App.tsx:208`. | Daemon-global events can refresh or annotate the wrong tab if they omit `workspaceId`. |
| Single active chat target in renderer. | `activeId` state at `packages/ui/app/App.tsx:68`; `sendOzMessage(oz, activeId, text)` at `packages/ui/app/App.tsx:272`. | No parallel/global Oz conversation target exists; loaded workspace tabs share one active chat dispatch path. |
| Daemon chat supports missing workspace only for limited commands. | `readMessage()` makes `workspaceId` optional at `packages/daemon/src/oz-chat.ts:229`; mutating commands return `missingWorkspace()` at `packages/daemon/src/oz-chat.ts:126`; `status` without workspace lists all runs at `packages/daemon/src/oz-chat.ts:220`. | There is no explicit contract separating "global Oz" from "workspace Oz"; absence means either all-run status or error depending on command. |
| Per-workspace in-flight lock assumes a workspace has one shared git working tree. | `ctx.inFlight.has(workspaceId)` at `packages/daemon/src/launcher.ts:469`; context comment at `packages/daemon/src/context.ts:87`; runner uses `workspace.path` as the active checkout at `packages/core/src/runner/runner.ts:324`. | Blocks concurrent runs within one workspace, but does not model separate writable roots inside a multi-root workspace. |
| Daemon restart, Oz repair, and authoring Plays use a global any-run guard. | `ctx.inFlight.size > 0` in restart at `packages/daemon/src/launcher.ts:918`, repair at `packages/daemon/src/launcher.ts:1017`, and authoring Play at `packages/daemon/src/launcher.ts:1062`. | A run in workspace A blocks daemon repair/authoring actions for workspace B. |
| Dashboard launcher singleton. | `dashboardLauncher.current` at `packages/daemon/src/context.ts:23`; duplicate launch refused at `packages/daemon/src/launcher.ts:927`. | One daemon-owned dashboard launch state spans all workspaces/windows. |
| Dirty-tree guard assumes the workspace primary repo is the commit surface. | `workspaceRepo = workspace.path` at `packages/core/src/runner/runner.ts:324`; dirty scope partition over `git.changedFiles(workspaceRepo)` at `packages/core/src/runner/runner.ts:335`; branch guard at `packages/core/src/runner/runner.ts:325`. | Multi-root workspaces or split writable roots have no independent dirty guard/lock; everything is measured from primary path. |
| Commit gate commits the whole changed tree for the chosen repo path. | `runCommitGate()` reads `git.changedFiles(cwd)` and commits `changed` at `packages/core/src/commit-gate/gate.ts:66`; `Git.addAndCommit()` stages exact files in one repo at `packages/core/src/commit-gate/git.ts:143`. | Concurrent actors sharing the same checkout can contaminate each other's commits unless the daemon lock prevents them. |
| cmux group map is keyed only by run id. | `#groups = new Map<string, string>()` at `packages/session-hosts/src/cmux/driver.ts:49`; runner passes `group: run.id` at `packages/core/src/runner/runner.ts:423` and `packages/core/src/runner/runner.ts:449`. | Safe only while run ids are globally unique. Per-workspace run ids require a namespaced group key. |
| cmux creation expects exactly one new workspace/pane during a before/after diff. | `diffNewWorkspace()` throws unless one new ref appears at `packages/session-hosts/src/cmux/launch.ts:24`; cmux driver uses it during split at `packages/session-hosts/src/cmux/driver.ts:69`. | Parallel cmux automation outside this driver can make the differ ambiguous. |
| cmux surface refs require workspace scoping, but stored session rows mix portable and local refs. | `send/read-screen/send-key` require `--workspace` at `packages/session-hosts/src/cmux/driver.ts:97`; `session.workspace_ref` schema at `packages/core/src/store/schema.ts:41`. | Durable session history cannot be moved as-is to tracked workspace files without separating local surface refs. |
| Registry storage is install-local and globally enumerated. | `workspaceDirectory(cocoderHome)` returns `local/workspace` at `packages/daemon/src/registry.ts:57`; `readWorkspaces()` uses the directory if any entries exist at `packages/daemon/src/registry.ts:167`. | Workspace identity/routing is not portable with the workspace and is shared by one install. |
| Priority/ticket creates use filesystem scans and temp names inside the workspace governance dir. | Priority duplicate scan at `packages/daemon/src/routes.ts:566`; ticket id scan at `packages/core/src/tickets/loader.ts:103`; ticket temp paths use `process.pid` and `Date.now()` at `packages/daemon/src/routes.ts:611`. | Parallel creates in the same workspace can race between scan and rename; today the single active UI path and human cadence reduce but do not eliminate it. |

## Notes For Next Design Step

- Do not make a prompt-only change for workspace segmentation. The current contract is spread across the registry, daemon routes, runner store, run-dir paths, UI active workspace state, SSE hints, and cmux labels.
- The first owner decision is whether run identity is global (`run_135` remains install-wide) or workspace-local (`cocoder/run_135` vs `other/run_135`). That decision controls DB migration, run directory layout, cmux group keys, and dashboard deep links.
- Portable history proposals above should be reviewed as a schema/taxonomy decision before any migration or runtime edits.
