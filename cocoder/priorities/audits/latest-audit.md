# Priority Set Audit - 2026-06-29

> **Superseded update 2026-06-30:** the founder clarified that `priority-audit` is the intended
> **Process Review** pinned launcher, expanded to include tickets. The former active
> `priority-audit.md` priority has been archived as absorbed by
> [`priority-panel-pinned-items`](../priority-panel-pinned-items.md); it is no longer a separate
> standing launchable priority.

Audited 15 site-set items: 6 keep-active, 1 archive, 3 redefine, 1 merge, 1 promote, and 3 demote recommendations. Headline: the `order.json` mismatch is mostly explainable (`adhoc-session` is intentionally unlisted; `AGENTS.md` is governance, not a priority), but the most important conflict is completed runner-decoupling work still sitting in backlog as two files (`runner-decoupling-progress.md` vs `runner-decoupling-refactor.md`). The original 2026-06-29 audit recommended keeping `priority-audit` active; that recommendation is superseded by the 2026-06-30 Process Review absorption decision above.

| Priority/item name | Status-vs-reality read | Recommended disposition | One-line reason | Dangling reference or conflict found |
|---|---|---|---|---|
| `priority-audit` | Superseded 2026-06-30 by `priority-panel-pinned-items` as the pinned **Process Review** launcher, expanded to include tickets. The archived file preserves the former standing-priority text for history. | archive/absorbed | The audit machinery still exists, but it now belongs to Process Review rather than a separate launchable priority. | none after absorption; remove from active `order.json` and top-level priority listing. |
| `new-primary-root` | File claims onboarding machinery is built and code backlog complete, with remaining founder-owned live proof beats; ADR-0020/0026/0032 and code/proofs support that, and `order.json` still lists it as a live priority. PLAYBOOK Phase 5 still needs an external repo proof. | keep-active | Objective is not complete until the founder-run external onboarding proof lands, but the file correctly says no ordinary build atoms remain. | none |
| `ad-hoc-session-architecture` | File claims it should produce an ADR for no-priority/ad-hoc run semantics and make no runner/UI code changes. Code already has the `adhoc-session` pseudo-priority, Oz `adhoc` command, ad-hoc prompt handling, and UI ad-hoc rows, but no dedicated ADR resolving the "Needs decision" vocabulary exists. It is in `order.json`. | keep-active | Keep as the design/ADR run that reconciles already-built partial behavior rather than editing the runtime pseudo-priority directly. | **MUST-FLAG: `ad-hoc-session-architecture.md` vs `adhoc-session.md`** - overlapping concern, but not the same artifact; architecture priority should explicitly reconcile the runtime file. |
| `adhoc-session` | File claims a standing no-named-priority support/on-ramp path; runtime confirms it is an intentionally unlisted pseudo-priority (`INTENTIONALLY_UNLISTED_PRIORITY_IDS`), Oz maps `adhoc <task>` to it, prompts enforce read-mostly support, and daemon rejects empty ad-hoc tasks. It is absent from `order.json` by design. | keep-active | It is a runtime sentinel/tool, not queue work; keep it active but unqueued. | **MUST-FLAG: `order.json` omits `adhoc-session.md`** - intentionally unordered, not an orphan. |
| `AGENTS.md` | Directory listing includes it, but it has no priority frontmatter and is governance instructions for `cocoder/priorities/`; daemon/core tests explicitly skip `AGENTS.md` in priority scans. It is absent from `order.json` because it is not a launchable item. | keep-active | Keep in place as directory governance; do not archive or add to order. | **MUST-FLAG: `order.json` omits `AGENTS.md`** - intentionally unordered non-priority, not an orphan. |
| `oz-terminal` | File claims it will replace the chat-window stub with a real streaming Oz terminal. Reality is partial: daemon has a headless Oz tool loop (`POST /oz/messages`), Oz tools, coarse `/oz/events` SSE, drag/drop pointers, and live UI chat send; but fixture mode still uses `ozReply` plus a 650ms fake reply, there is no token-streaming `/oz/stream`, and xterm/session-host architecture is unresolved. | keep-active | The command surface is real, but the core "real streaming terminal" verified-when is not met. | Conflict between file's streaming-terminal objective and current non-streaming request/response chat plus fixture stub. |
| `agentic-pattern-drift-detection` | Backlog file claims the shipped Drift Audit catches stale paths but not retired behavior patterns; archived `drift-audit` and current drift code/proofs support that. No ADR/objective pass has made this launchable, and precision risk remains high. | demote | Keep deferred until the existing drift detector is calm enough to add agentic enrichment without noise. | none |
| `deployment-plays` | File claims deployment Plays are Phase 5 work; PLAYBOOK still puts external repo validation in Phase 5. Plays infrastructure exists, but no onboarded deploy target or secrets UX proof exists. | demote | Correctly deferred until a real external repo needs a human-gated deploy. | none |
| `multi-repo-commit-spine` | Backlog file claims a future per-repo commit spine, but its own notes say framing predates ADR-0023. ADR-0045 now sharpens the model further: workspace/root is the hard boundary, intra-root path scope is advisory, and multi-root commit semantics need a fresh current-truth design. | redefine | Valid future seam, but must be rewritten against ADR-0023/0045 before promotion. | Stale worktree-isolation framing and unratified ADR-0019 amendments. |
| `priority-architecture-contract` | File claims every launchable priority should carry a machine-checkable Governing Architecture section; ADR-0010/0035/0038 instead make Objective/placement the live priority contract, and the PLAYBOOK cautions against governance-of-governance gates unless earned. It also records a prior opportunistic implementation that was reverted. | redefine | Needs a narrower boundary-protection Objective before it should become a launch gate. | Risk of duplicating priority authoring taxonomy and bricking existing priorities. |
| `quinn-app-testing` | Backlog file claims browser-test Play is Phase 5 work; ADR-0033 keeps Quinn as the experience-QA persona and tests show `electron-test` exists, but no `browser-test` Play or browser app target exists. | demote | Correctly deferred until an onboarded browser app exists. | Reference to moved `personas-and-plays` history should be refreshed when touched. |
| `research-sandboxing` | File claims research should default to "not now" and wait for the commit spine to be reliable. ADR-0006 still says trust CLI permissions for MVP; ADR-0045 now says root/workspace is the hard boundary and intra-root scope is advisory. The file's old commit-time holdback language is stale after ADR-0023/0045. | redefine | Keep the research intent, but refresh the premise before launch. | Stale claim that write-scope is a commit-time hold-back model rather than commit-and-flag/advisory inside the root. |
| `run-tests-required-checkpoint` | Backlog file claims tests are discretionary culture, not a structural gate. ADR-0033 makes testing a Play capability and shared standards demand evidence, but code atoms still depend on per-atom criteria rather than a universal code-touching checkpoint. | promote | This is a real quality-gate gap for onboarded repos without CI; promote for an ADR-backed minimal checkpoint design. | Needs escape hatch for docs-only/no-test repos so the gate does not become launch-blocking theater. |
| `runner-decoupling-progress` | File is a progress ledger for `runner-decoupling-refactor`; top entry declares "WS5 COMPLETE / runner decoupling refactor COMPLETE", "priority DONE", "Exact next step: NONE". It is 1,562 lines and duplicates the same completed concern as the spec file. | merge | Merge/archive with the refactor record instead of leaving a huge progress ledger as backlog work. | **MUST-FLAG: `runner-decoupling-progress.md` vs `runner-decoupling-refactor.md`** - same priority history; no next work remains. |
| `runner-decoupling-refactor` | File claims a non-orchestrated refactor priority; progress ledger says all five done-when conditions are met, with verification recorded and no next session needed. Code reality shows runner remains large but split into modules (`triage.ts`, `status.ts`, retention modules, etc.) per the accepted "irreducible orchestration shell" assessment. | archive | The priority is complete by its own ledger and should stop appearing as backlog work. | Same completed concern as `runner-decoupling-progress.md`; archive together after founder approval. |
