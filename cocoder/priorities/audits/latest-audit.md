# Priority Set Audit — 2026-06-16

Audited 11 items: 5 top-level active priorities and 6 backlog priorities. Three look stale or archive-ready now (`play-dispatch-boundary`, `oz-held-back-expand-scope`, and possibly the orphaned `hybrid-plays` slot if the founder does not want it queued). Highest-value decision first: clean the active set so top-level files match the intended launch surface, then continue `new-primary-root`.

Recently completed context, not scored: `governance-authoring-plays` is archived after proof 8/8 and both agent/human author-then-launch paths met the objective (`cocoder/priorities/archive/governance-authoring-plays.md:42`, `:75`, `:79`); `headless-adapter-lane` is archived after Claude/Codex headless proof and capability flip (`cocoder/priorities/archive/headless-adapter-lane.md:47`, `:51`, `:58`); `oz-dashboard-bugs` is archived after all 12 defects were fixed and founder-confirmed (`cocoder/priorities/archive/oz-dashboard-bugs.md:58`, `:60`, `:72`).

| Priority | Status vs reality | Recommended disposition | One-line reason | Dangling refs / conflicts |
|---|---|---:|---|---|
| `play-dispatch-boundary` | Resolved decision record is still a top-level active file; it says it is "no longer a queued priority" and the slot was repointed to `hybrid-plays` (`cocoder/priorities/play-dispatch-boundary.md:7`, `:8`, `:9`). | archive | The decision is complete: one-level dispatch stands, no ADR-0024 or engine reversal is required (`cocoder/priorities/play-dispatch-boundary.md:21`, `:47`). | Active orphan: absent from `order.json`, which lists only `new-primary-root` and `priority-audit` (`cocoder/priorities/order.json:1`). |
| `oz-held-back-expand-scope` | Backlog file is explicitly cancelled; the premise no longer exists because the commit spine does not withhold out-of-lane edits (`cocoder/priorities/backlog/oz-held-back-expand-scope.md:4`, `:7`, `:9`). | archive | ADR-0023 Amendment 1 deleted held-back / `pending-scope-decision`; out-of-lane edits commit and are flagged (`cocoder/decisions/0023-workspace-commit-spine.md:76`, `:80`, `:81`; code confirms in `packages/core/src/commit-gate/gate.ts:47`, `:58`, `:60`). | The obsolete objective still describes `pending-scope-decision` and `expand` (`cocoder/priorities/backlog/oz-held-back-expand-scope.md:16`, `:20`, `:22`). |
| `hybrid-plays` | Real follow-on idea, but the objective is still marked DRAFT and the active file is not in the launch order (`cocoder/priorities/hybrid-plays.md:23`; `cocoder/priorities/order.json:1`). | demote | The claimed gap is true today: `Play` has only `id/label/kind/writeScope/body` and `dispatchPlay` injects markdown into a prompt, with no deterministic step field (`packages/core/src/plays/types.ts:5`, `:10`, `:12`; `packages/core/src/plays/dispatch.ts:99`, `:100`). | Active orphan: absent from `order.json`; founder should either add it after `new-primary-root` or move it to backlog until wanted. |
| `new-primary-root` | Correct top launchable priority; ADR-0020 is accepted and the engine foundation is partially built, but the P2->P5 executor and live proofs remain (`cocoder/priorities/new-primary-root.md:6`, `:46`, `:74`, `:79`). | keep-active | It is the only active build priority in `order.json`; loader/scaffold/deep-read are code-backed, while executor and proofs are still open (`cocoder/priorities/order.json:2`; `packages/core/src/playbooks/loader.ts:62`; `packages/daemon/src/routes.ts:256`, `:259`; `packages/core/src/scaffold/scaffold.ts:59`). | File still says not to relaunch build atoms until D2 lifts or executor is designed (`cocoder/priorities/new-primary-root.md:89`); Playbook says D2 dependency is now satisfied by `headless-adapter-lane` (`cocoder/PLAYBOOK.md:214`). Reconcile before launching. |
| `priority-audit` | Standing meta-priority is doing exactly this run; it must remain available (`cocoder/priorities/priority-audit.md:11`, `:18`, `:22`). | keep-active | Its objective is a founder-decision table across active/backlog against current built state, and it says it is never archived (`cocoder/priorities/priority-audit.md:11`, `:13`, `:26`). | None; present in `order.json` (`cocoder/priorities/order.json:3`). |
| `adhoc-session` | Standing support/on-ramp priority, not build work; objective is bounded read-mostly review/research or priority drafting (`cocoder/priorities/adhoc-session.md:6`, `:8`, `:11`). | keep-active | PLAYBOOK names it as an always-available standing tool alongside `priority-audit` (`cocoder/PLAYBOOK.md:223`, `:224`, `:225`). | Active orphan by manifest: absent from `order.json`; likely intentional standing-tool exception, but the manifest does not encode that exception (`cocoder/priorities/order.json:1`). |
| `multi-repo-commit-spine` | Valid future slice, but stale framing needs refresh against the post-reset commit spine and no launchable Objective exists (`cocoder/priorities/backlog/multi-repo-commit-spine.md:3`, `:13`, `:18`). | redefine | ADR-0019 covers multi-root identity, but ADR-0023 now owns the one direct-to-branch commit contract; pickup must restate this as "one spine per managed root," not per-root worktree isolation (`cocoder/decisions/0019-multi-root-workspaces.md:24`, `:37`, `:39`; `cocoder/decisions/0023-workspace-commit-spine.md:87`, `:94`, `:103`). | Needs an Objective + ADR amendment before runnable, per its own file and PLAYBOOK (`cocoder/priorities/backlog/multi-repo-commit-spine.md:4`; `cocoder/PLAYBOOK.md:236`). |
| `priority-architecture-contract` | Placeholder backlog item; still founder-owned and explicitly blocked on Objective + ADR-conflict pass (`cocoder/priorities/backlog/priority-architecture-contract.md:6`, `:15`). | redefine | The proposal risks governance-of-governance unless rescoped to a concrete launch boundary; PLAYBOOK says to re-scope before any build (`cocoder/PLAYBOOK.md:238`, `:239`). | Must not be implemented opportunistically; origin notes prior slipped implementation was reverted (`cocoder/priorities/backlog/priority-architecture-contract.md:18`, `:21`, `:22`). |
| `deployment-plays` | Correctly deferred; generic Plays are built, but there is no Phase-5 repo/deploy target yet (`cocoder/priorities/backlog/deployment-plays.md:14`, `:15`, `:16`). | demote | Objective depends on a real founder-approved deploy of an onboarded repo with keys from `local/secrets`; CoCoder itself does not satisfy that (`cocoder/priorities/backlog/deployment-plays.md:7`, `:9`, `:17`). | No conflict; promote only when Phase 5 has a deployable repo. |
| `quinn-app-testing` | Correctly deferred to Phase 5 browser-app work; base Quinn/electron-test scope already moved elsewhere (`cocoder/priorities/backlog/quinn-app-testing.md:12`, `:16`, `:17`). | demote | There is no browser app to drive yet; code check shows `electron-test` exists but no `browser-test` Play is present (`packages/personas/base/plays/electron-test.md:2`; `find packages/personas/base/plays -maxdepth 1 -type f -name '*.md'`). | Its link points to `../personas-and-plays.md`, which is now archived per PLAYBOOK, so update that reference when the backlog item is next touched (`cocoder/PLAYBOOK.md:177`, `:179`). |
| `research-sandboxing` | Valid research spike but intentionally blocked until the commit spine is boringly reliable; current posture is "not now" by default (`cocoder/priorities/backlog/research-sandboxing.md:6`, `:7`, `:17`). | demote | The hard constraints say no happy-path friction and off-by-default opt-in only; ADR-0023 was just amended to remove constraints from the happy path (`cocoder/priorities/backlog/research-sandboxing.md:26`, `:34`, `:35`; `cocoder/decisions/0023-workspace-commit-spine.md:55`, `:76`). | Lines 46-48 still describe the old commit-time hold-back model; refresh them before any research run because code now commits and flags (`cocoder/priorities/backlog/research-sandboxing.md:46`, `:47`; `packages/core/src/commit-gate/gate.ts:5`, `:48`). |

## Dangling references & conflicts

- Active top-level priorities absent from `order.json`: `adhoc-session`, `hybrid-plays`, and `play-dispatch-boundary`. `play-dispatch-boundary` is a true orphan; `adhoc-session` is probably a standing-tool exception; `hybrid-plays` needs a founder queue decision (`cocoder/priorities/order.json:1`; `cocoder/priorities/AGENTS.md:39`, `:40`).
- `play-dispatch-boundary` should not remain in the active launch glob because it explicitly says the question is resolved and the slot moved to `hybrid-plays` (`cocoder/priorities/play-dispatch-boundary.md:7`, `:9`).
- `new-primary-root` contains an internal stale gate: it says not to relaunch until D2 lifts, while PLAYBOOK says the headless-lane dependency is satisfied and ticket 0006 closed (`cocoder/priorities/new-primary-root.md:89`; `cocoder/PLAYBOOK.md:214`).
- `oz-held-back-expand-scope` is cancelled but still in backlog; its Objective describes states now removed from `RunStatus` (`cocoder/priorities/backlog/oz-held-back-expand-scope.md:16`; `packages/core/src/store/types.ts:6`, `:12`).
- `research-sandboxing` still describes write-scope as commit-time hold-back for tracked changes; current code says scope is advisory and records `out-of-scope-committed` instead (`cocoder/priorities/backlog/research-sandboxing.md:46`; `packages/core/src/commit-gate/gate.ts:5`, `:60`).
- Stale code/docs references found during spot-check: `packages/core/src/runner/runner.ts:128`, `:468`, `packages/core/src/runner/prompts.ts:381`, and `packages/ui/BUILD_PROMPT.md:102` still mention held-back / `pending-scope-decision` language, while `RunStatus` no longer includes that state (`packages/core/src/store/types.ts:12`). These are outside this audit's write-scope but should be cleaned by a machinery/docs priority.
- `quinn-app-testing` references `../personas-and-plays.md`, but that priority is archived in the PLAYBOOK; update the pointer when the item is next revised (`cocoder/priorities/backlog/quinn-app-testing.md:16`; `cocoder/PLAYBOOK.md:177`, `:179`).
- `cocoder/priorities/AGENTS.md` is not a misplaced priority: it is governance instructions for the priority directory, not a Playbook, and the daemon explicitly skips non-priority `.md` like `AGENTS.md` (`cocoder/priorities/AGENTS.md:1`, `:39`; `packages/daemon/src/routes.ts:270`).
