{
  "contract": "persona-contract",
  "version": 1,
  "status": "phase-2-draft",
  "description": "Model-neutral contract for a CoCoder build persona.",
  "required": [
    "id",
    "label",
    "mode",
    "role",
    "launchModel",
    "writePolicy",
    "resultContract",
    "evidenceResponsibilities",
    "reviewStatus"
  ],
  "fields": {
    "id": { "type": "string" },
    "label": { "type": "string" },
    "mode": { "enum": ["orchestrator", "writer", "bounded-writer", "verifier", "script-qa"] },
    "role": { "type": "string" },
    "launchModel": { "enum": ["long-lived-visible", "one-shot", "script-only", "helper"] },
    "writePolicy": { "type": "string", "description": "References write-boundary contract id or read-only." },
    "allowedRoutes": { "type": "array", "items": "route id" },
    "resultContract": { "type": "string" },
    "evidenceResponsibilities": { "type": "array", "items": "string" },
    "reviewStatus": { "enum": ["draft", "founder-reviewed", "persona-owner-reviewed", "canonical"] }
  },
  "rules": [
    "Persona contracts in this directory are rewrites, not copies of private playbooks.",
    "Bob sessions must not open private non-Bob persona playbooks while drafting.",
    "Contracts that change behavior require founder approval.",
    "Contracts become canonical only after the required founder/persona-owner review gate."
  ],
  "initialPersonas": ["bob", "oscar", "talia", "quinn", "ian", "phil"]
}
