{
  "contract": "write-boundary",
  "version": 1,
  "status": "phase-2-draft",
  "description": "Positive and negative write scopes for personas, helpers, and routes.",
  "required": [
    "id",
    "mode",
    "allowed",
    "excluded",
    "enforcement"
  ],
  "fields": {
    "id": { "type": "string" },
    "mode": { "enum": ["read-only", "task-scoped", "bounded", "repo"] },
    "allowed": { "type": "array", "items": "path prefix or explicit file" },
    "excluded": { "type": "array", "items": "path prefix or explicit file" },
    "enforcement": { "type": "array", "items": "preflight|post-diff|result-contract|review-gate" },
    "requiresFounderApproval": { "type": "boolean" }
  },
  "rules": [
    "Read-only jobs must not create, edit, delete, move, format, or stage repo files.",
    "Writer jobs must report filesChanged.",
    "Post-run diff audit must compare filesChanged against allowed and excluded scopes.",
    "Helper jobs require disjoint write sets before dispatch."
  ]
}
